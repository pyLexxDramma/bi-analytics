server {
    listen 443 ssl;
    server_name opencode.ai.conall.ru;
    ssl_certificate     /etc/letsencrypt/live/opencode.ai.conall.ru/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/opencode.ai.conall.ru/privkey.pem;
    ssl_protocols       TLSv1.2 TLSv1.3;
    client_max_body_size 64m;
    location / {
        proxy_pass http://10.35.15.75:4096;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_buffering off;
        proxy_read_timeout 3600s;
        proxy_send_timeout 3600s;
    }
}
server {
    listen 80;
    server_name opencode.ai.conall.ru;
    return 301 https://$host$request_uri;
}
