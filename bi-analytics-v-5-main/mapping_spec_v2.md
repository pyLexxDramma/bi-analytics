# mapping_spec_v2

## 1) Цель

Зафиксировать рабочую схему маппинга данных для BI-дашбордов с учетом:
- фактических полей выгрузок в `web/` и `web/AI/`;
- файла сверки `Правки/Отчеты для сверки.xlsx`;
- замечаний из двух видео встречи от `24.04.2026`.

Документ ориентирован на внедрение в текущий код проекта.

### 1.1 Где лежат данные в репозитории
- **`web/`** — рабочая папка выгрузок для приложения: 1С JSON, TESSA CSV, справочники; подкаталог **`web/AI/`** — MSP и «прочие» (`other_*_resursi`, `other_*_rd` и т.д.). Соглашения имён и типы файлов для ETL: **`etl/parser.py`** (докстринг в начале файла).
- **`etl/`** — `parser` / `mapper` / `loader` / `schema` (SQLite `etl.db` при использовании пайплайна). Чеклист п. **5.1**.
- **`new_csv/`** — примеры структуры для тестов/витрин; **не** являются эталоном для сверки с `Правки/Отчеты для сверки.xlsx` (эталон — фактические файлы в **`web/`** / **`web/AI/`**).
- **`Правки/Отчеты для сверки.xlsx`** — вне репозитория или копия рядом с `Правки/`; листы для приёмки — п. **6**.
- **Архив `mapping_spec` (синхрон):** в папке `Analitics` держится копия `fixes 24.04.2025/mapping_spec_v2.md` с теми же отметками; после изменений **этого** файла в git обновляют копию и **push** (см. «Правило» в шапке архива).

## 2) Канонические источники и поля

### 2.1 MSP (сроки, причины, ПД, контрольные точки)
Источник: `web/AI/msp_*_<date>.csv`

Ключевые поля:
- `ID_проекта`, `ЛОТ`, `БЛОК`, `Уровень`, `Название`, `Ид`, `Уникальный_идентификатор`
- `Базовое_начало`, `Базовое_окончание`, `Начало`, `Окончание`
- `Процент_завершения`, `Причины_отклонений`
- `Отклонение_начала`, `Отклонение_окончания`

### 2.2 TESSA ID (ИД и предписания)
Источник: `web/tessa_*_id.csv`

Ключевые поля:
- `DocID`, `KindName`, `KrState`, `KrStateID`, `CreationDate`
- `1C_ID_PODR`, `1C_ID_CONTR`, `1C_ID_LOT`
- `CONTR`, `ObjectName`, `Lot`, `DocNumber`, `DocDescription`

### 2.3 TESSA RD (РД-факт)
Источник: `web/tessa_*_rd.csv`

Ключевые поля:
- `DocID`, `KrState`, `CreationDate`
- `ObjectName`, `ObjectProjectID`, `ObjectProjectName`
- `DivisionCipher`, `SubDivisionVersionName`
- `CONTR`, `Lot`

### 2.4 TESSA Tasks (статусы задач, причина/результат)
Источник: `web/tessa_*_task.csv`

Ключевые поля:
- `CardID`, `TypeCaption`, `OptionCaption`, `Result`, `Completed`

### 2.5 1C финансы
Источники:
- `web/*_dannye.json` (БДДС/оборотные)
- `web/*_DK.json` (ДЗ/КЗ)
- `web/*_spravochniki.json` (справочники 1С; **для ГДРС** этот же файл — источник **плановых** значений по согласованию с заказчиком, см. **§2.6**)

Ключевые поля (минимум в текущих выгрузках; план ГДРС — расширение/иные узлы в том же `spravochniki` — по схеме 1С):
- `dannye.json`: `Период`, `Сценарий`, `СтатьяОборотов`, `Проект`, `Сумма`, `РасходДоход`, `ТипСтатьи`
- `DK.json`: `Организация`, `Контрагент`, `Договор`, `ОстатокНаНачалоПериода`, `ОстатокНаНачалоПериодаПоАвансам`, `ВсегоОплат`, `ВсегоОплат_Аванс`, `ОстатокНаКонецПериода`, `ОстатокНаКонецПериодаПоАвансам`
- `spravochniki.json`: контрагент, договор, проект; плановые **ГДРС** в выгрузке 27.03.2026 — `КоличествоРаботников` / `КоличествоСпецТехники` (часто **массив** `[{Дата, Количество}, ...]`; в коде — агрегат, см. `web/EXPORTS_REFERENCE.md` и `_gdrs_merge_plan_from_1c_spravochniki`). Первые строки файла могут содержать только реквизиты контрагента без плановых полей.

### 2.6 ГДРС/СКУД
**План (плановые значения, люди/техника):** `web/1c_*_spravochniki.json` — **источник плана** для ГДРС: `КоличествоРаботников` (рабочие) и `КоличествоСпецТехники` (техника) привязаны к **контрагент / договор / проект** (ссылки в JSON). Склейка с фактом: `Наименование_Контрагента` ↔ `Подрядчик` и (по возможности) `Наименование_Проекта` ↔ `Проект` (нормализация имён, **§3.1**).

**Факт (СКУД, динамика по дням/неделям):** `web/AI/other_*_resursi.csv` (примеры: `other_01-01-2026_resursi.csv`, `other_01-02-2026_resursi.csv`).

Ключевые поля (факт):
- `Проект`, `Подрядчик` (в коде встречается и сопоставление с `Контрагент`);
- `тип ресурсов` — `рабочие` / `техника` (и варианты написания); на вкладке «ГДРС» (рабочие) в референсной таблице отчёта оставляются строки **только с рабочими**, техника выносится в меню **«ГДРС Техника»**;
- при наличии в выгрузке — колонка **`data_source`** (семантика: строки «Ресурсы» / «Техника») для согласования с вкладками; если колонки нет, тип определяется по структуре файла и `тип ресурсов` (см. `dashboard_workforce_movement`);
- ежедневные столбцы дат (`ДД.ММ.ГГГГ`), при необходимости — колонки **«N неделя»**, агрегаты вида `среднее значение ...`, `среднее за месяц` / `среднее за неделю` (имена — по фактическому файлу).

**Сверка:** листы Excel **`ГДРС (вычищен, факт, январь)`** / **`ГДРС (сводная, факт, январь)`** в `Правки/Отчеты для сверки.xlsx` — эталон агрегатов **факта**; план в приёмке — согласовать с полями в **`1c_*_spravochniki.json`** после внедрения чтения плановых величин в коде.

### 2.7 РД-план
Источник: `web/AI/other_*_rd.csv`

Ключевые поля:
- `Номер`, `Шифр`, `Наименование раздела`
- `Количество разделов по Договору`
- `Дата выдачи разделов по Договору`

## 3) Сквозные ключи и правила связи

### 3.1 Приоритет ключа проекта
1. `MSP.ID_проекта = TESSA_RD.ObjectProjectID`
2. fallback по имени проекта (`ObjectName`/`Проект`) после нормализации

Нормализация строкового ключа:
- trim, collapse пробелов, casefold;
- удаление служебных кавычек/невидимых символов;
- дедупликация дублей названий проекта в фильтрах.

### 3.2 Предписания/ИД: связь карточки и задачи
- Базовая связь: `TESSA_TASK.CardID = TESSA_ID.DocID`
- При неполных строках применять объединение по нормализованному ключу DocID/CardID.

### 3.3 РД-план ↔ РД-факт
- Ключ связи: `Проект + DivisionCipher(Шифр) + SubDivisionVersionName(Наименование раздела)`
- Если нет `Дата выдачи разделов по Договору` в плане, строку не показывать в плановом слое графика РД.

### 3.4 Статусы
- Расшифровка статусов: через `KrStates.csv`
- Для предписаний исключать служебный статус `KrState = Проект` из KPI и таблиц.

## 4) Маппинг по отчетам (что исправить по видео)

## 4.1 Контрольные точки / График проекта / Сроки
- [x] Маппинг проекта: канонизация `project name` (§**3.1**) в **«Контрольные точки»**, **«График проекта»**, **«Отклонение от базового плана»** — без дублей в фильтрах/агрегатах; прочие правки (даты, сортировка, уровни) — по плану.
- Для дат использовать только пары:
  - План: `Базовое_начало`, `Базовое_окончание`
  - Факт: `Начало`, `Окончание`
- В таблицах переименовать плановые поля в базовые.
- Добавить/починить сортировку отклонений по клику заголовка.
- Починить фильтр уровней (2/3/4/5) и чекбокс скрытия завершенных задач.

## 4.2 Причины отклонений
- Единые фильтры: `Проект`, `ФБ/БЛОК`, `Строение/уровень`, `Период`, `ТОП-N причин`.
- [x] После применения фильтров — канонизация `project name` (§**3.1**), чтобы таблицы и агрегаты не дублировали варианты одного проекта; [x] то же в **«Доли причин»** / **«Динамика отклонений»** / **«Динамика причин»** при отдельном открытии.
- Корректная обработка периода с частичными данными (без падений).
- Привести таблицу и stacked-диаграммы к логике сверки (подписи/доли/сортировка).

## 4.3 ГДРС и СКУД
- [x] Убрать дублирующий экран/таб (`СКУД по неделям`) — удалён `dashboard_skud_stroyka`, график рабочих в «ГДРС».
- Базовый экран движения рабочей силы строить по `тип ресурсов = рабочие` (для референсной таблицы на вкладке «Ресурсы» — фильтр строк по рабочим, см. **§2.6**).
- [x] Вкладка **«ГДРС Техника»:** подзаголовок референсной таблицы и срез `data_source` не должны оставаться на текст «люди»/только «Ресурсы», если открыт отчёт техники (согласовано с заголовком отчёта).
- Иерархия таблицы: `Проект -> Подрядчик` (и детализация по виду работ при наличии колонки).
- Отклонение считать как `Факт(СКУД) - План`.
- [x] **План (код):** `load_all_from_web` → `reference_contractors`; `_gdrs_merge_plan_from_1c_spravochniki` — `КоличествоРаботников` / `КоличествоСпецТехники` (в т.ч. JSON-массивы), строка факта с `План_numeric==0`; `other_*_resursi.csv` без колонки «План» — план с 1С, см. **`web/EXPORTS_REFERENCE.md`**.
- [x] **Референсная таблица «люди/техника»:** после `groupby` по суточным колонкам сутки исчезают — недели/СКУД из **`week_sum`** (равномерный сплит по числу недель месяца), иначе были нули.
- [x] **Период с/по (без суточных колонок в `work_df`):** если выбранный диапазон **не пересекается** с месяцем данных — **не** обнулять строки; `suppress_caption` + факт/план как в данных; дефолт **min/max** из `work_df[Период]`, не только `date.today()`.
- [x] **Мультиселект проектов** — сопоставление с строками по **`_project_filter_norm_key`**, не только «сырая» строка.

### 4.3.1 Диагностика: почему в «ГДРС» по **рабочим** не видны **План** и **Дельта (%)**
Заметки для приёмки и отладки (не баг UI, если нет данных в цепочке):

| Симптом | Вероятная причина |
|--------|-------------------|
| **План** везде `0`, **Дельта (%)** — «—» | Нет загрузки `1c_*_spravochniki.json` → `st.session_state['reference_contractors']` пустой; merge `_gdrs_merge_plan_from_1c_spravochniki` не из чего подставить план. Проверить `load_all_from_web`, наличие файла в `web/`. |
| То же | В JSON нет поля **`КоличествоРаботников`** (или имя не совпало с алиасами в `_gdrs_spravochnik_ref_column`) — смотреть фактические ключи объекта в справочнике. |
| То же | **Контрагент** или **проект** в `other_*_resursi.csv` не сходятся со строкой справочника по `norm_partner_join_key` / `_project_filter_norm_key` (другая форма названия, нет пары контрагент+проект в 1С). |
| **Техника** с планом, **люди** без | В справочнике заполнено **`КоличествоСпецТехники`**, по **`КоличествоРаботников`** — пусто или не те строки. |
| **Дельта (%)** = «—» при ненулевом факте | План по строке = **0** → деление на план для % **не определено**; показывается «—» (ожидаемо до появления плана в данных). |

Код: ref-таблица — `_gdrs_coalesce_plan_for_gdrs_table` (приоритет: договор/resursi «План» → `План_numeric` после merge); merge — `_gdrs_merge_plan_from_1c_spravochniki`.

## 4.4 Проектные работы (РД/ПД)
- [x] Статус `Передано подрядчику` — фильтр + доля TESSA/MSP, KrState/колонка; круговая «Исполнение РД».
- [x] Поле «Задача» в детальной таблице РД — TESSA (`_task_display_tessa`), иначе MSP.
- [x] Мультивыбор проектов — `dashboard_rd_delay` / `dashboard_documentation`.
- [x] Круг/линия РД: доли `Не принято` / `Не выдано` + «Передано подрядчику»; линия: старт 0, план `#2E86AB`, факт `#F39C12`, подписи % и «пр.» (отставание накоп.) на точках.

## 4.5 Исполнительная документация
- Перепроверить заполнение KPI и таблиц через цепочку `tessa_id + tessa_task + KrStates`.
- Цвета статусов табов привести к ТЗ.
- Даты в UI показывать без времени.
- [x] Фильтр «Объект»: один пункт на нормализованный ключ (как **§3.1**), сопоставление строк — по `_project_filter_norm_key`, не по «сырому» равенству строк.
- В фильтрах оставить обязательный чекбокс: "не показывать просрочку, если ИД сдана".

## 4.6 Неустраненные предписания
- [x] Диапазон дат **выдачи** (`CreationDate` → `_issue_date`) — в фильтрах; добавлен диапазон по **сроку устранения** (`_due`), если в TESSA есть колонка срока.
- [x] KPI (кольца/метрики): всего, устранённые, неустранённые, просроченные, критические — единая логика в `dashboard_predpisania` (`_pred_kpi_circles_html` + расчёт `_resolved`, `_overdue_days`, `_critical`).
- Исключать `KrState=Проект`.
- Дедуп по ключу предписания (`DocID`, fallback `CardID`) обязателен.
- [x] Стабильный порядок групп на графике / `uirevision` — правки R23-10 (без «скачков» при refresh на тех же данных).

## 4.7 Финансы
- [x] В `DK` использовать расчетные поля (в **`dashboard_debit_credit`**, если в выгрузке есть пары колонок):
  - `ОстатокНаНачало = ОстатокНаНачалоПериода - ОстатокНаНачалоПериодаПоАвансам` → в UI: **«Остаток на начало (нетто)»**
  - `ОстатокНаКонец = ОстатокНаКонецПериода - ОстатокНаКонецПериодаПоАвансам` → **«Остаток на конец (нетто)»**; иначе, как раньше, одна колонка **«Остаток на период»** из файла
- [x] В «Прогнозном бюджете» убран дублирующий нижний HTML-блок «детальные строки» (данные остаются в `st.data_editor`); экспорт лотов в CSV/Excel — под сводной таблицей (**§4.7**, 2026-04-24).
- [x] **Утверждённый бюджет** (`dashboard_approved_budget`, график план/факт по месяцам): как **БДДС** — скрытие месяцев без сумм, легенда снизу, цвета и отступы столбцов, читаемые подписи (2026-04-24).

## 4.8 UI: «плотные колонки/столбцы» (таблицы + графики) — **на всех дашбордах**
- **Таблицы (HTML/`st.dataframe` где применимо):** не оставлять визуально «пустую» ширину колонок; для фиксированного layout задавать классы/ширины по смыслу (даты/ID/суммы — уже, текст — шире с переносом); избегать `min-width`, который растягивает таблицу «в пустоту`, если контента мало; горизонтальный скролл — нормально, но **цель** — у пользователя сразу видна «привязка» значения к заголовку.
- **Графики/диаграммы (в первую очередь Plotly):** оси/категории не должны выглядеть как «широкий зазор без причины»; для bar/horizontal bar — контролировать `bargap`, `margin`, длину подписей (перенос/угол), при необходимости `tickangle`, `tickformat`, `automargin` и/или `uirevision` (как сейчас в части отчётов) так, чтобы подписи и значения **читались без угадывания**, а легенда не «съедала» полезную площадь.
- **Единообразие:** при добавлении нового отчёта — повторять тот же UX‑принцип: компактные числовые/датовые поля, аккуратные длинные строки, предсказуемые отступы.

## 4.9 UI: без «серых» дебаг/служебных подсказок — **на всех дашбордах**
- **Не показывать** мелкий серый текст `st.caption` под заголовками/таблицами/графиками, если это служебное пояснение, «запись N», отладка источника данных или дублирует то, что уже на экране. В коде для production используется заглушка (`dashboards/ui_quiet.py`: `suppress_caption`) вместо `st.caption`.
- **`st.expander`** — не для дебаг-диагностики и длинных инструкций «как устроено внутри»; при необходимости пояснить — через согласованный с заказчиком блок (без дублей под графиками).
- **`st.info` / `st.warning` / `st.error`** — оставлять для реальных состояний (нет данных, ошибка загрузки, нарушение фильтра); это **не** относится к пункту про «серые» подсказки.
- **Про `help=` у виджетов** — отдельный пункт плана: **4.10** (снятие/согласование).

## 4.10 UI: снятие лишних `help=` с фильтров и виджетов — **отдельная задача внедрения**
- **Цель:** в интерфейсе нет «второй линии» пояснений через иконку `?` у `st.selectbox` / `st.multiselect` / `st.checkbox` и т.п., если это внутренняя памятка разработчику, дублирует лейбл или противоречит 4.9.
- **Допустимо:** краткие продуктовые `help=` строго по согласованному макету/ТЗ (редко, единообразно).
- **В коде:** пройти `dashboards/_renderers.py` (и при необходимости другие экраны), удалить/сжать многострочные `help=(...)`, внутренние пояснения про структуру MSP/иерархию — вынести в документацию поставщика данных, не в UI.

## 4.11 UI: подписи на **всех** диаграммах и графиках (видны сразу, порядок, читабельность)
- **Видимость:** основные подписи категорий, рядов, значений и ключевых меток должны читаться **с первого взгляда**, без обязательного наведения. **Hover** в Plotly — только дополнение (детализация, длинный текст), а не единственный способ понять, что на графике.
- **Расположение:** единая логика — не «лес» из налезающих лейблов; при необходимости: `text`/`textposition` на бар/точки, `tickangle` / перенос длинных подписей оси, `automargin`, предсказуемые `margin`, при пересечениях — прореживание тиков (`nticks`, `tickmode`, сокращение длинных подписей с `…` + полный текст в hover).
- **Числа и единицы:** формат осей и подписей над столбцами/секторами — в одном стиле с отчётом (разделители, `%`, сокращения); избегать наезда текста на ряд/столбец (`uniformtext`, разумный `minsize` или сокращение).
- **Легенда:** если ряды не очевидны с оси/подписей — легенда читаема, не перекрывает график (положение/строка/столбец по шаблону отчёта), цвета согласованы с макетом.
- **Новые отчёты:** копировать тот же подход, что в уже вылизанных графиках (см. вспомогательные `apply_chart_background`, `_apply_bar_uniformtext`, раскладки finance-баров и т.д.).
- **Код (централизация):** `render_chart` вызывает `_apply_plotly_spec_411_labels` (Bar: подписи значений, если не задан `text`/`texttemplate`, без Gantt/дат по эвристике; Pie: при отсутствии подписи — `textinfo` с `label+percent` при необходимости); в `utils.apply_chart_background` для `uniformtext` используется `mode="show"` (и согласовано с `_apply_bar_uniformtext` / рядом `update_layout` в отчётах).
- **Приёмка:** пункты §5.2 / §6 по 4.11 отмечены `[x]` после ответа **«ok»** (2026-04-24).

## 5) Чеклист внедрения по файлам/функциям

## 5.1 ETL: распознавание/маппинг источников
- [ ] `etl/parser.py`
  - [x] `detect_file()` — фактические `web/`: `other_{project}_DD.MM.YYYY_rd`, `1с_…_dannye|DK|spravochniki.json` (префикс **1** + лат. **c** / кирил. **с**), `tessa_…_id|rd|task`, `msp_…`, `other_*_resursi`, `KrStates`/`…`; правки 2026-04-24.
  - [x] `group_by_snapshot()` + `load_from_web_dir` — `__unknown__` (нет даты в имени) → явная ошибка/пропуск, цикл только по `YYYY-MM-DD`; докстринг 2026-04-24.
- [ ] `etl/mapper.py`
  - [ ] `map_msp()` — нормализация дат/отклонений и колонок MSP.
  - [ ] `map_tessa_id()` / `map_tessa_rd()` / `map_tessa_task()` — контроль ключей `DocID/CardID`, `KrStateID`, `ObjectProjectID`.
  - [ ] `map_resources()` — разделение `рабочие/техника`, корректная агрегация дат.
  - [ ] `map_rd_plan()` — контроль пустой плановой даты.
  - [ ] `map_1c_budget()` / `map_1c_dk()` / `map_1c_sprav()` — числовые поля и кодировки.
- [ ] `etl/loader.py`
  - [ ] `_resolve_kr_states()` — корректная русификация статусов.
  - [ ] порядок загрузки ссылочных и версионных данных.

## 5.2 Реестр дашбордов и меню
- [x] `dashboards/__init__.py`
  - [x] убрать/скрыть дублирующие пункты в ГДРС/СКУД (отдельный пункт «СКУД стройка» / экран «СКУД по неделям» убран из реестра).
  - [ ] проверить алиасы названий отчетов.
  - [x] обновить `_DASHBOARDS_REGISTRY_VERSION` (39 — §4.4 круг/линия РД, TESSA-задача, «Передано подрядчику»; v38: `_rd_tessa_task_display_series` + проект; v37: …). Правки только текста **mapping_spec** реестр не меняют.
- [x] `dashboards/ui_quiet.py` — `suppress_caption` (без `st.caption` в дашбордах, см. 4.9).
- [ ] `dashboards/_renderers.py` / `dev_projects_tz_matrix.py` — капшены/подсказки: только п. 4.9.
- [x] `dashboards/_renderers.py` — **снятие `help=`** по п. **4.10** (все `help=` в файле убраны; админ/профиль/главная при необходимости отдельно).
- [x] Все графики/диаграммы — подписи и раскладка по п. **4.11** (центрально: `render_chart` / `_apply_plotly_spec_411_labels`, `apply_chart_background`; приёмка **ok** 2026-04-24).

## 5.3 Сроки / причины / контрольные точки / график проекта
- [ ] `dashboards/_renderers.py`
  - [x] `dashboard_control_points()` — [x] канонизация `project name` (§**3.1**).
  - [x] `dashboard_project_schedule_chart()` — [x] то же.
  - [x] `dashboard_plan_fact_dates()` — [x] то же.
  - [x] `dashboard_deviations_combined()` — [x] в `_render_deviations_combined_shared_filters` канонизация колонки `project name` для таб/графиков.
  - [x] `dashboard_reasons_of_deviation()` — [x] канонизация `project name` (§**3.1**), в т.ч. вне общего отчёта.
  - [x] `dashboard_dynamics_of_deviations()` — [x] то же.
  - [x] `dashboard_dynamics_of_reasons()` — [x] то же.

## 5.4 ГДРС и СКУД
- [ ] `dashboards/_renderers.py` (закрыть после сверки с Excel-эталоном)
  - [x] `dashboard_workforce_movement()` — **факт** из `web/AI/other_*_resursi.csv`; **план** — `КоличествоРаботников` / `КоличествоСпецТехники` + merge; **релиз 2026-04-24:** ref-таблица `week_sum`→недели, период без пересечения, проекты по норм. ключу (**§4.3**); [x] канонизация колонки **«Проект»** (как **§3.1**); [x] при **нет** `reference_contractors` и факт `week_sum`≠0, план=0 — одно `st.info` с отсылкой к **§4.3.1**. **Если План/Дельта % пустые** — см. **§4.3.1**. Сверка с `Отчеты для сверки.xlsx` — [ ].
  - [x] `dashboard_skud_stroyka()` — удалён (дубль 4.3).
  - [x] `dashboard_technique_tabs()` — обёртка: заголовок + `dashboard_workforce_movement(..., Ресурсы)`; **«ГДРС Техника»** — `dashboard_gdrs_equipment()`.
  - [x] `dashboard_workforce_and_skud()` — удалён (не использовался в реестре).
  - [x] `dashboard_gdrs_equipment()` — отдельный пункт меню; см. 4.3.

## 5.5 РД/ПД
- [ ] `dashboards/_renderers.py` (сверка Excel / приёмка — [ ])
  - [x] `dashboard_rd_delay()` / `dashboard_pd_delay()` — [x] **§3.1**, мультивыбор, TESSA-«Задача» в детализации, линия «Просрочка»/см. `dashboard_documentation` для план/факт линий
  - [x] `dashboard_working_documentation()` / `dashboard_project_documentation()` / `dashboard_documentation()` — [x] **§4.4** (круг «Исполнение РД», динамика, колонка «Передано подрядчику» в find_column)
  - [x] `_rd_tessa_task_display_series()` + колонка «Задача» в детальной таблице — **§4.4**

## 5.6 ИД и предписания
- [ ] `dashboards/_renderers.py`
  - [ ] `dashboard_executive_documentation()` — [x] фильтр `ObjectName` / объекта: список и отбор по нормализованному ключу (**§3.1**); детальная таблица — даты `дд.мм.гггг`.
  - [ ] `dashboard_predpisania()` — [x] фильтры по периоду выдачи и сроку устранения; [x] KPI/графики по 4.6; [ ] расширения по ТЗ/цветам при появлении макета.
  - [ ] `_tessa_fill_card_from_doc_lookup()`
  - [ ] `_pred_dedupe_by_docid()`
  - [ ] `_pred_merge_completion_from_tasks()`

## 5.7 Финансы
- [ ] `dashboards/_renderers.py`
  - [x] `dashboard_budget_by_period()` — [x] читаемость графика (нулевые месяцы, отступы, подписи) — 2026-04-24; сверка — [ ]
  - [x] `dashboard_bdr()` — [x] то же; сверка — [ ]
  - [x] `dashboard_budget_by_type()` — [x] канон. `project name` (**§3.1**); сверка — [ ]
  - [x] `dashboard_forecast_budget()` — [x] канонизация проекта (**§3.1**); [x] без дублирующего нижнего HTML по лотам (**§4.7**); сверка с эталоном — [ ]
  - [x] `dashboard_debit_credit()` — [x] нетто-остатки ДК по **§4.7** при наличии колонок; иначе fallback на «Остаток на период».

## 6) Контроль приемки (минимум)

- [ ] Сверка с `Правки/Отчеты для сверки.xlsx` по листам (имена листов — по факту в книге; при расхождении переименования обновить этот список):
  - **ГДРС:** `ГДРС (вычищен, факт, январь)` и/или `ГДРС (сводная, факт, январь)` — согласовать с заказчиком, какой лист **обязателен** для приёмки;
  - **СРОКИ:** `СРОКИ (Дмитровский, исходный)` / `СРОКИ (Дмитровский, сводная)` (и аналоги по проектам при появлении);
  - **TESSA:** `TESSA (ИД, исходный)`, `TESSA (ИД, сводная)`, `TESSA (РД, исходный)`, `TESSA (РД, сводная)`, `TESSA (задачи, исходный)`, при необходимости `TESSA (маппинг справочников)`.
  - **РД (доп.):** `РД (исходный, дополнительный)`, `РД (дополнительный, таблица)` — по объёму правок.
- [ ] На одинаковом наборе входных файлов повторный refresh не меняет KPI.
- [ ] В фильтрах нет дублей проектов.
- [ ] Нефинансовые метрики отображаются целыми числами.
- [ ] В таблицах включен горизонтальный скролл, даты без лишнего времени.
- [ ] **Единые правила «колонок/столбцов» для всех отчётов (таблицы + графики):** визуально «плотная» сетка без «пустых» широких колонок; даты/коды/числа — компактно; длинные подписи в таблицах — перенос/`title` на ячейке при необходимости.
- [x] **Подписи на графиках/диаграммах (4.11):** основной смысл (категории, значения, ряды) виден **без** обязательного наведения; подписи не наезжают хаотично; hover — только уточнение. Соответствие п. 4.8 (плотность, легенда, отступы). **ok** 2026-04-24.
- [ ] **Нет «серых» служебных подсказок (4.9):** `st.caption` в дашбордах не ведёт к видимому тексту (`suppress_caption`); нет expander'ов с дебаг-инструкциями. Сообщения `st.info` / `st.warning` / `st.error` — только по делу.
- [x] **«Серых» `help=` в отчётах нет (4.10):** в `dashboards/_renderers.py` аргументы `help=` сняты. При необходимости — снять в `pages/*`, `project_visualization_app.py`.

## 7) Примечание по внедрению

Сначала закрывать блоки в приоритете:
1. `Предписания` и `ИД` (наибольшее расхождение по KPI у заказчика),
2. `Сроки/Причины`,
3. `РД/ПД`,
4. `ГДРС`,
5. `Финансы`.

**Текущий фокус (2026-04-24):** **5.1** ETL (`detect_file` — [x], далее `group_by_snapshot`/mapper/loader), **§6** сверка, **4.8** плотность. Реестр **39** (посл. смена — §4.4).

