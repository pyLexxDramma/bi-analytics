# Журнал правок (пошагово)

**Проект:** BI Analytics  
**Начато:** 2026-04-16



## Шаг 1 — «Правки 1» (скрин / ТЗ): Контрольные точки

**Статус:** сделано (ожидает ручной проверки)

### Правка

**Файлы:** `dashboards/dev_projects_tz_matrix.py`, `dashboards/_renderers.py`

1. **Убраны фильтры** «Этап» и «Функциональный блок» на экране «Контрольные точки». Остались **Проект** и при наличии колонки — **Строение** (`_apply_control_points_msp_filters`).
2. **Статус:** вместо символов ✓/✗ — **цветовые круги** (зелёный = ок по датам, красный = отклонение / неполные даты), подсказка в `title`.
3. **Оранжевая подсветка** ячеек План / Факт / Откл. для вех **ГПЗУ** (`gpzu`) и **Экспертиза стадия стП** (`exp_pd`), если у сопоставленной задачи MSP **% выполнения ≠ 100%** (колонка `pct complete` после загрузки). Константа `CONTROL_POINTS_ORANGE_PCT_SLUGS`.
4. **Дубли проекта:** строки с разными подписями одного логического проекта **Дмитровский** («Дмитровский», «Дмитровский-1» и т.п.) **объединяются** в одну строку таблицы с подписью из `MSP_PROJECT_NAME_MAP` / «Дмитровский 1» (`_control_points_project_group_key`, `_control_points_project_label`, `build_control_points_df`).
5. В CSV-выгрузке скрыты служебные поля `*_warn_pct` (как и `*_ok`).
6. Подсказка в UI обновлена под новые фильтры и логику.

**Продолжение (шаг 2):** админ-интерфейс для маппинга вех и заголовков — см. **Шаг 2**.

---

## Шаг 2 — Админка «Контрольные точки (MSP)» + единый фильтр проекта

**Статус:** сделано (ожидает ручной проверки)

### Правка

**Файлы:** `pages/admin.py`, `dashboards/dev_projects_tz_matrix.py` (ранее)

1. В **Административная панель** добавлена вкладка **«Контрольные точки (MSP)»**: редактирование JSON массива вех `{ "title", "slug", "match" }` — **заголовки столбцов** отчёта и **правила сопоставления** со строками CSV MS Project (поле `match` как во встроенном коде). Сохранение в настройки БД по ключу **`control_points_milestones_json`**; кнопки «Сохранить», «Сбросить на встроенные правила», выгрузка шаблона по умолчанию.
2. **Дубли «Дмитровский» / «Дмитровский-1»** в фильтре и таблице: одна логическая группа (`_control_points_project_group_key` / `_control_points_project_label`, `_control_points_project_filter_options`).

### Проверка (ручная)

<!-- Под админом: сохранить JSON, открыть отчёт «Контрольные точки», убедиться в заголовках и отсутствии дублей Дмитровского -->

### Заметки

- Пустой JSON при сбросе = встроенный список `CONTROL_POINT_MILESTONES` из кода.

#### Как администратору настроить отчёт «Контрольные точки» (маппинг к CSV MS Project)

1. Войти под ролью **администратор** или **суперадминистратор**.
2. В боковом меню открыть **Общие настройки** (страница `pages/admin.py`).
3. Перейти на вкладку **«Контрольные точки (MSP)»**.
4. В поле JSON задать **массив вех**. У каждой вехи:
   - **`title`** — заголовок группы столбцов в отчёте (то, что видят пользователи).
   - **`slug`** — внутренний ключ (латиница, без пробелов); по нему строятся поля в данных (`slug_plan`, `slug_fact`, …). Менять slug после накопления данных стоит осторожно.
   - **`match`** — правила отбора строк из загруженного MSP (как в коде: `level`, `names_any`, `name_contains`, `parent_l2_contains`, `block_contains`, `phase_needles`, `phase_exclude_needles` и т.д.). Это и есть **сопоставление ячеек отчёта с задачами в CSV MS Project**.
5. Нажать **«Сохранить»**. Настройка пишется в БД по ключу **`control_points_milestones_json`**.
6. **«Сбросить на встроенные правила»** — очистить ключ и снова использовать вехи из кода (`CONTROL_POINT_MILESTONES`).
7. **«Скачать шаблон по умолчанию»** — эталонный JSON для правок.



## Шаг 3 — «График проекта» (макет правок)

**Статус:** сделано (ожидает ручной проверки)

### Правка

**Файлы:** `dashboards/_renderers.py` (`dashboard_project_schedule_chart`, вспомогательные `_render_gantt_schedule_html_table`, …), `utils.py` (`apply_chart_background`), `project_visualization_app.py` (CSS сайдбара)

1. **Фильтры:** в одной строке — проект (ур. 1), функциональный блок (ур. 2), уровень отображения MSP (3 / 4 / 5 или **«Все уровни»** по умолчанию), режим подписей у концов полос (**дата окончания** / **% выполнения**). Ниже строкой — **«Показать причины отклонений»** и **«Отображать в лотах»** (если в данных есть колонка лота).
2. **Гант:** `px.timeline` для плана + второй trace для базы (`barmode=group`); подписи у концов полос — аннотации; ось X с русскими подписями дат/месяцев; при необходимости — опция «в лотах» и поиск колонки уровня/лота по алиасам.
3. **Ось Y:** отступы `margin.l` / `ticklabelstandoff`; **не** используется CSS `translateX` по ширине margin — иначе подписи задач уезжают за край и нечитаемы.
4. **Таблица** (expander «Таблица (первые строки)» под подписью к графику): фиксированный порядок колонок — **Проект**, **Задача**, **План начало** / **План окончание**, **База: начало** / **База: окончание**, при наличии — **% выполнения**, всегда — **Отклонение Начала** и **Отклонение Окончания** (поля отклонения MSP или разница план − база в днях, формат «±N дн.»). При включённом **«Показать причины отклонений»** — **Причины отклонений** и **Заметки** (если колонки есть в выгрузке). Подсветка ячеек отклонений (задержка / опережение) через `Styler`, как в блоке отклонений по дням.
5. **Plotly:** `apply_chart_background(..., skip_uniformtext=True)` для этого графика — иначе глобальный `uniformtext mode=hide` может скрывать подписи у полос.
6. **Навигация:** скрыт стандартный список страниц Streamlit (`[data-testid="stSidebarNav"]`); переходы — через меню приложения.

### Проверка (ручная)

- График проекта: фильтры, план/база двумя цветами, подписи у концов полос, таблица с нужными заголовками и отклонениями; при включении чекбокса — колонки причин/заметок.
- Подписи задач слева на диаграмме читаемы (не «полоска»).


---

## Шаг 4 — «Значения отклонений от базового плана»: фильтры по уровням MSP

**Статус:** сделано (ожидает ручной проверки)

### Правка

**Файл:** `dashboards/_renderers.py` — `dashboard_deviation_by_tasks_current_month`, вспомогательные `_dev_tasks_find_column`, `_dev_tasks_resolve_level_column`, `_dev_tasks_build_ancestor_keys`

1. Убран **«Фильтр по этапу»** (колонка `section`).
2. **Функциональный блок** — список задач **уровня 2** по колонке уровня MSP (имена задач с `Outline Level` / `level` = 2).
3. **Строение** — задачи **уровня 3**; при выбранном блоке список ограничивается строками, у которых предок ур. 2 совпадает с выбранным блоком (обход иерархии по порядку строк MSP).
4. **Детализация** — переключатель **уровень 4 (укрупнённо)** / **уровень 5 (детально)**; в выборку попадают только строки с соответствующим уровнем структуры.
5. Если колонка уровня не найдена — подсказка и fallback: при наличии колонок **Функциональный блок** / **Строение** в файле — фильтры по ним; без уровней 4/5.
6. Блок **«Детализация отклонений по задачам»** использует **те же** фильтры, что основной график; группировка по `task name` без «Раздела».

### Проверка (ручная)

- Экран «Значения отклонений от базового плана»: нет «Фильтр по этапу»; четыре колонки фильтров (проект, блок, строение, детализация) ведут себя по ТЗ на типичной выгрузке MSP.

### Заметки

- Иерархия считается по **порядку строк** в загруженном датафрейме (как в типичном экспорте MSP). При нестандартной сортировке исходного файла списки блок/строение могут потребовать уточнения.

---

## Шаг 5 — «Отклонение от базового плана» (скрины / ТЗ)

**Статус:** сделано (ожидает ручной проверки)

### Правка

**Файлы:** `dashboards/_renderers.py` (`dashboard_plan_fact_dates`, `_render_html_table`, вспомогательные для графиков/таблицы), `settings.py` (ключ `baseline_plan_task_for_metrics`), `pages/admin.py` (вкладка **«Контрольные точки (MSP)»**, внизу страницы — подзаголовок **Отчёт «Отклонение от базового плана»** и поле **Задача для расчёта окончания проекта (MSP)** с сохранением через `set_setting`)

1. **Фильтры и UI:** четыре колонки в первой строке; **«Только диаграммы с отклонением окончания меньше 0»** вынесен отдельно; **детализация** — подписи «Укрупнённо (уровень 4)» / «Детально (уровень 5)»; **«Скрыть завершённые (100%)»** без `disabled`, с подсказкой если колонки % нет; **«Подписи задач на графике»** — «По наименованию MSP» / «По лоту» (если есть колонка лота). Фильтр проекта с `_session_reset_project_if_excluded` / `_project_name_select_options`.
2. **Верхние KPI** («Максимальное отклонение», план/факт окончания) и блок **«РС»** — **убраны** с экрана отчёта.
3. **Админка:** настройка **«Задача для расчёта окончания проекта»** вынесена на вкладку **«Контрольные точки (MSP)»** (доступ только администраторам), ключ БД **`baseline_plan_task_for_metrics`** (`set_setting` / `get_setting` из `settings`).
4. **Диаграмма по этапам:** горизонтальные столбцы, сортировка этапов по **убыванию** величины отклонения; подпись под графиком обновлена.
5. **График план/факт и подготовка `viz_data`:** подпись задачи через **`_bar_task_label_from_row`** (лот или MSP + санитайз строк «инженерные сети №1/№2»).
6. **Ковенанты:** таймлайн (маркеры plan end / base end) и таблица с колонками **Базовое окончание**, **Окончание**, **Отклонение окончания** (как в ТЗ по смыслу колонок в выгрузке).
7. **Таблица «Отклонение от базового плана (таблица)»:** колонки **Базовое начало** / **Базовое окончание** (плановые даты MSP); **Начало (факт)** / **Окончание** (вместо «Факт Начало/Конец»); отклонения — **Отклонение начала**, **Отклонение**, **Отклонение длительности** (короткие имена по ТЗ). Колонка **«Раздел»** (`section`) выводится **только** при подписях **«По наименованию MSP»** и наличии поля в выгрузке. При наличии в файле — **ID задачи (MSP)**. Чекбокс **«Только строки с отклонением окончания > 0»** (по умолчанию вкл.), в `help` — формула **base end − plan end** (дни). Сортировка в селекте: варианты **«Отклонение — …»** (вместо «Отклонение конца»); для старых сессий — миграция `st.session_state["dates_sort_order"]`.
8. **`_render_html_table`:** **`column_tooltips`** у заголовков; **`column_role`** — классы `col-baseline` / `col-fact` / `col-dev` для лёгкой раскраски столбцов; у ячеек **`td`** при необходимости **`title`** («колонка: значение»). Чекбокс **«Таблица: сортировка по клику (встроенная)»** — вместо HTML показывается **`st.dataframe`** с **`style_dataframe_for_dark_theme`** (числовые отклонения, подсветка дат план/факт и колонки «Отклонение»).
9. **ЗОС:** мини-таблица — колонки **Базовое окончание** / **Окончание**; к имени задачи применён тот же санитайз «инженерные сети».
10. **Expander «Откуда берутся сроки…»** — текст без описания верхних KPI; упоминание админки для задачи метрик.

### Проверка (ручная)

- Отчёт «Отклонение от базового плана»: фильтры, чекбоксы, подписи MSP/лот на графиках, горизональная диаграмма по этапам, таблица с переименованными колонками и подсказками по заголовкам, ID MSP при наличии в CSV.
- Админка: сохранение **задачи для расчёта окончания проекта**, повторное открытие — значение на месте.
- Режим ковенантов (при данных): таймлайн и таблица ковенантов.
- ЗОС: колонки и читаемость.

### Заметки

- Семантика дат в коде: **`plan start` / `plan end`** — плановые даты в таблице подписаны как **«Базовое начало/окончание»**; **`base start` / `base end`** — факт (**«Начало (факт)»** / **«Окончание»**). Отклонение окончания в днях: **`plan_end_diff` = base end − plan end** (колонка **«Отклонение»** после форматирования).
- График **«План/факт по этапам»:** `render_chart(..., skip_clamp_zoom=True)` — без глобального «clamp» зума, который ломал пустую/узкую ось дат.
- При следующих правках по этому экрану — обновить этот шаг и перечень файлов в блоке **Правка**.

---

## Шаг 6 — «Причины отклонений»: фильтры по иерархии MSP, причина у таблицы, динамика по месяцам

**Статус:** сделано (ожидает ручной проверки)

### Правка

**Файлы:** `dashboards/_renderers.py` (`_render_deviations_combined_shared_filters`, `dashboard_reasons_of_deviation`, `dashboard_dynamics_of_deviations`, `dashboard_dynamics_of_reasons`), `project_visualization_app.py` (текст приветствия)

1. **Общие фильтры** отчёта «Причины отклонений»: вместо «Этап» и глобальной «Причины» — **функциональный блок** (задачи **уровня 2**) и **Строение** (задачи **уровня 3**) по той же иерархии MSP, что в «Значения отклонений…» (`_dev_tasks_build_ancestor_keys`, ключи `_dt_lvl2_key` / `_dt_lvl3_key`). При отсутствии колонки уровня — fallback на колонки **block** и колонку строения из файла.
2. **Период** «с / по» — вторая строка фильтров (как раньше по смыслу).
3. **«Причина»** перенесена в блок **«Детальные данные»** (`reason_filter_table_only`) — влияет только на таблицы по макету, полную выгрузку и CSV; **диаграммы и метрики** считаются по общим фильтрам без отбора по причине.
4. Вкладка **«Динамика по периодам»** переименована в **«Динамика отклонений по месяцам»**; подзаголовок и подсказки обновлены.
5. На stacked bar **«По причинам»** (дни отклонений по периоду и причинам) добавлены **подписи внутри сегментов** столбцов (`textposition='inside'`).
6. Вспомогательные функции: `_drop_deviation_hierarchy_artifacts`, `_deviations_apply_block_building_filters`, `_deviations_project_slice_by_key`.

### Проверка (ручная)

- Общие фильтры: блок/строение из иерархии (при наличии уровня в MSP); период; переключение вкладок не сбрасывает выбор.
- Первая вкладка: диаграммы без фильтра по причине; таблицы — с селектом «Причина».
- Вторая вкладка: заголовок «Динамика отклонений по месяцам»; на stacked bar видны числа внутри сегментов.

### Заметки

- Старые ключи `devcombo_section` / `devcombo_reason` в session_state больше не используются в общих фильтрах.

---

## Шаг 7 — «Причины отклонений»: полная таблица по макету + тренд на stacked bar

**Статус:** сделано (ожидает ручной проверки)

### Правка

**Файл:** `dashboards/_renderers.py`

1. **Полная выгрузка по фильтрам** (expander под «Детальные данные»): колонка **«Этап»** убрана; порядок и подписи по ТЗ:
   - **Начало** / **Базовое начало** (факт / план), **Отклонение начала** (дни) = план начало − факт начало; шрифт **красный** если &lt; 0, иначе зелёный.
   - **Окончание** / **Базовое окончание**, **Отклонение окончания** (дни) = факт окончание − план окончание; **красный** если &gt; 0, иначе зелёный.
   - **Базовая длительность** / **Длительность** (дни между соответствующими датами).
   - **Причина отклонений**, **Заметки** из MSP; при наличии — **Дата снимка**.
2. **Цвета фона колонок:** бирюза — группа начала; синий — окончание; тёмно-синий — длительности; светло-голубой — ячейки отклонений (`_DEV_REASONS_FULL_TABLE_CSS`).
3. Рендер: **`_render_deviations_reasons_full_table`**; CSV с тем же порядком колонок.
4. **Динамика отклонений по месяцам**, блок **«По причинам»** (stacked bar): добавлена **красная линия тренда** по сумме «Всего дней отклонений» по периоду (`go.Scatter`); суммарные аннотации над столбцами убраны во избежание дубля с линией.

### Проверка (ручная)

- Полная таблица: колонки, порядок, подсветка, отклонения и CSV.
- Stacked bar + красная линия итога по периодам.

### Заметки

- Нумерация недель на оси X (как на референсе) не внедрялась — остаётся текущая группировка периода из селекта «Группировать по».

---

## Шаг 8 — Меню отчётов: порядок групп, «Проектные работы», фильтр девелоперских проектов

**Статус:** сделано (ожидает ручной проверки)

### Правка

**Файлы:** `dashboards/__init__.py` (`REPORT_CATEGORIES`, версия реестра), `dashboards/_renderers.py` (`dashboard_developer_projects`), `project_visualization_app.py` (текст приветствия)

1. **Порядок групп в боковом меню:** Девелоперские проекты → Сроки → Финансы → **Проектные работы** → **ГДРС** → **Исполнительная документация** (отдельная группа) → Предписания по подрядчикам.
2. Группа **«Документация»** переименована в **«Проектные работы»**; внутри только **Рабочая** и **Проектная** документация. **Исполнительная документация** вынесена в самостоятельную группу (как «Девелоперские проекты»).
3. Группа **«ГДРС»:** отчёты **ГДРС**, **График движения рабочей силы**, **СКУД стройка** (раньше «График движения…» был отдельной верхней группой).
4. **Девелоперские проекты:** в UI оставлен **только фильтр «Проект»** (убраны раздел, блок, строение, лоты).

### Проверка (ручная)

- Меню: порядок и названия групп; исполнительная документация отдельно; проектные работы без исполнительной внутри.
- Девелоперские проекты: только селект проекта; матрица при «Все» и нескольких проектах — прежнее предупреждение.

### Заметки

- Версия кэша дашбордов `_DASHBOARDS_REGISTRY_VERSION` увеличена.

---

## Шаг 9 — «Проектные работы» / ПД–РД: фильтры, графики, таблица план/факт

**Статус:** сделано (код в `main`)

### Правка

**Файл:** `dashboards/_renderers.py`

1. **Рабочая / проектная документация (`dashboard_documentation`):** период **«За всё время»** / **«Диапазон дат»** (по умолчанию весь период); фильтр **«Фильтр по виду раздела РД»**; круговая **«Исполнение РД»** (доли **Выдано** / **Не принято** / **Не выдано**); **«Динамика выдачи РД»** по плановой и фактической дате выдачи, накопительный график с нуля, **План** — синий (`#2E86AB`), **Факт** — оранжевый (`#F39C12`).
2. **Просрочка выдачи РД (`dashboard_rd_delay`)** и вкладка ПД через **`dashboard_pd_delay`:** фильтр по разделу РД; горизонтальные бары с градиентом зелёный → жёлтый → красный; таблица **«План и факт окончания ПД/РД»** без колонки задачи (проект + направление раздела + даты); **«Динамика по месяцам»** — стек **Выполнено** / **Разница план/факт** / **Просрочено** (в процентах к плану месяца).
3. В **`dashboard_project_documentation`** вкладка переименована в **«Просрочка выдачи РД»** (вместо «ПД» в смысле отдельной вкладки просрочки).
4. **Фильтр по проекту:** `_project_filter_norm_key`, `_unique_project_labels_for_select` — один пункт selectbox на один нормализованный ключ (скрытые дубли вроде пробелов/NBSP); **«Дмитровский»** и **«Дмитровский-1»** по-прежнему разные ключи без бизнес-слияния.

### Проверка (автоматическая)

- `python -m py_compile` по `dashboards/_renderers.py` — без ошибок; линтер — без замечаний (по отчёту разработчика).

---

## Шаг 10 — «Исполнительная документация» (TESSA): таблица, статусы, сравнение по месяцам, UI

**Статус:** сделано (код в `main`)

### Правка

**Файл:** `dashboards/_renderers.py` — `dashboard_executive_documentation`, вспомогательные `_exec_*`, CSS `_EXEC_DOC_DETAIL_CSS`

1. **Детальная таблица:** **«Дата создания»** — только дата без времени (`%d.%m.%Y`); просрочки сдачи и согласования считаются по дням стабильнее; при чекбоксе скрытия для подписанных ИД — «—»; **сортировка по клику на заголовок** (параметры `exec_sort` / `exec_order` в query string).
2. **Статусы (бейджи):** **Отказ** — красный, **Принят** — зелёный, **У подрядчика** (доработка) — голубой; у заказчика — жёлтая гамма (`_exec_status_pill_html` + классы `exec-pill-*`).
3. **«Изменения за месяц к предыдущему»:** выбор месяца, KPI по выбранному месяцу и **дельты** к предыдущему (абсолютное изменение и процент).
4. **Фильтры:** объект, контрагент, вид документа, период по дате создания; дополнительно — один чекбокс **«Не отображать просрочку, если ИД сдана (подписана/согласована)»**; прочие старые флаги убраны.
5. **Оформление блока ИД:** панель `exec-doc-panel`, крупные KPI-карточки (`exec-kpi-grid`), поясняющие подписи под таблицей и блоками.

### Проверка (автоматическая)

- `python -m py_compile` — ок; линтер по файлу — без ошибок (по отчёту разработчика).

### Git (шаги 9–10)

- **Фиксация:** ветка `main`, коммит с сообщением **«ПД/РД, Исполнительная документация: шаги 9–10 + журнал»** — изменения в `dashboards/_renderers.py` и в этом файле `docs/PRAVKI_STEPS_LOG.md`.
- **SHA на `origin/main` (пакет кода + журнал):** `2abd4f7`.

---

## Шаг 11 — «Предписания по подрядчикам»: фильтры, график, KPI, детальная таблица

**Статус:** сделано (код в `main`)

### Правка

**Файл:** `dashboards/_renderers.py` — `dashboard_predpisania`, вспомогательные `_pred_*`, CSS `_PRED_DASH_MOCK_CSS`

1. **Фильтры:** **`Период выдачи предписаний`** (`st.date_input` по `_issue_date` из даты создания TESSA); чекбокс **«Не отображать устраненные предписания»** (по умолчанию вкл.); проект, подрядчик, поиск по № договора, кнопки «Применить» / «Сбросить».
2. **Основной график:** горизонтальные **оранжевые** столбцы — число **просроченных** по подрядчику; **синие круги** (`go.Scatter`) — **всего неустранённых**; увеличенная высота (`max(420, n*52+120)`), подписи, в **hover** — диапазон дат выдачи (мин–макс по `_issue_date`); легенда в HTML над графиком.
3. **KPI:** круги **«Всего предписаний»**, **«Неустраненные»**, **«Устраненные предписания»**, просроченные и критические; подпись с пояснением маппинга метрик.
4. **Детальная таблица:** колонки **Статус**, **Дата выдачи**, **Блок выдачи**, **Срок устранения**, **Фактическая дата устранения** и др. (`_PRED_DETAIL_TABLE_COLUMNS`); сортировка по клику на заголовок (`pred_sort` / `pred_order`); строки **розовые** — просрочка, **салатовые** — устранённые (`pred-row-overdue` / `pred-row-resolved`).
5. **Исправления отображения:** **критические** — «Да» / **«0»** вместо прочерков для не-критических (`critical_text`); пустые номера договора/предписания — **«Без номера»** (`_pred_fmt_num`); **просрочка** (`_overdue_days_row`) только для **не** устранённых (`_resolved` → 0 дней просрочки).

### Проверка (автоматическая)

- `python -m py_compile` — ок; линтер по файлу — без ошибок (по отчёту разработчика).

### Git (шаг 11)

- **Ветка:** `main`. Ищите коммит по сообщению (например, «предписания», «pred», «шаг 11») или по дате; затем `git show <hash> --stat` — затронут `dashboards/_renderers.py` и при необходимости этот файл журнала.


