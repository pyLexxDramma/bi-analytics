# Чеклист: отчёт «Девелоперские проекты» — правки от 2026-05-06

Эталон данных для сверок: **`bi-analytics-v-5-main/web/`** (MSP `AI/msp_*.csv`, выгрузки 1С `1с_*_dannye.json`, Tessa `tessa_*-*.csv`).

## Код и логика

- [ ] **1. Чекбокс вертикальных дат** — убран; даты только горизонтально (`dashboards/_renderers.py`: `vert_dates = False`; подсказка в админ-JSON обновлена).
- [ ] **2а. Предписания — только TESSA** — строка «ПРЕДПИСАНИЯ» из `tessa_tasks_data` с фильтром проекта (`_tessa_counts` + `build_predpisaniya_detail_df`), без падения назад на MSP/`Фаза`.
- [ ] **2б. Финансы — как БДДС** — «Выборка ДС» через `try_synthetic_budget_from_1c_dannye` по `reference_1c_dannye`, fallback на прежние обороты (`_dev_matrix_bddds_totals_mln`).
- [ ] **3. Дубликаты проектов (Дмитровский)** — в режиме «Все проекты» строки группируются ключом `_control_points_project_group_key`; в фильтре «Проект» — те же группы.
- [ ] **4. Оранжевая подсветка / % выполнения** — представитель вехи: задача с **минимальным** числовым `% complete` среди совпадений; tooltip на План/Факт при предупреждении.
- [ ] **5. РС** — расширены `names_any` / `names_exact_any` / `phase_needles` для задач РС в MSP.
- [ ] **6. Отклонения (дней)** — по-прежнему **«План» = базовое окончание, «Факт» = окончание**, отклонение календарных дней; при расхождениях с ожиданием сверить даты и поля MSP с `web/`.
- [ ] **7. Даты DD.MM.YYYY** — `_fmt_date_ru` уже с `dayfirst=True`; если останутся случаи 04.01 вместо 01.04 — зафиксировать исходное значение в CSV и доработать парсинг отдельно.
- [ ] **8. Жирное начертание** — усилены `font-weight` для «Проект», групп инвест./жизненной фазы и заголовков вех в CSS матрицы.

## Проверка в UI

- [ ] Streamlit: отчёт **«Девелоперские проекты»**, тема по умолчанию, режим «Все», затем по одному логическому проекту.
- [ ] Убедиться: нет UI-чекбокса вертикальных дат; шапка вех читается жирнее; при наведении на оранжевые даты — tooltip про % выполнения.
- [ ] Сверить блок **ПРЕДПИСАНИЯ** с задачами Tessa KindName=`Предписан…` для того же объекта проекта (`web/tessa_*-task*.csv`).
- [ ] Сверить **Выборка ДС, млн руб.** суммами с синтетикой БДДС для того же `reference_1c_dannye` / дампами `*_dannye.json`.
- [ ] Для Спорной вехи (РС, отклонения дней): открыть соответствующие строки в `web/AI/msp_*.csv` и проверить `Базовое окончание` / `% завершения` / название задачи.
- [ ] **БДДС / БДР** — графики и сводные таблицы по чеклисту раздела «БДДС / БДР» ниже (шрифт/колонки/«Итого»/подписи **млн. руб.**).

## БДДС / БДР (обороты 1С, `dashboards/_renderers.py`, `dashboards/finance_from_1c.py`, `utils.py`)

- [ ] **СтатьяОборотов** — в расчёт попадают только строки, где в названии статьи одновременно отражены **лот и подлот** (эвристика `_turnover_article_has_lot_and_sublot`, фильтр в `try_synthetic_budget_from_1c_dannye` / `try_synthetic_bdr_from_1c_dannye`).
- [ ] **Округление и подписи** — суммы в **млн руб.** с **двумя** знаками после запятой (`format_million_rub`, `_finance_bar_text_mln_rub`, таблицы БДДС/БДР); сверка с `python scripts/verify_web_finance_totals.py` по JSON из `web/`.
- [ ] **Строка «Итого»** — жирное выделение (`budget_table_to_html` + `emphasize_row_kinds`); при **«По месяцам»** — сумма по строкам периода; при **«Накопительно»** — значения **последнего периода** (БДДС: сводная внутри того же фрагмента, что и график; БДР ТЗ-таблица).
- [ ] **Таблица** — уже́е ячейки, **крупнее шрифт** в `budget_table_to_html` (обёртка бюджетных таблиц).
- [ ] **Порядок колонок** в сводных таблицах БДДС и БДР (ТЗ): **Период → Проект → План → Факт → Отклонение** (+ опционально скорр. бюджет в БДДС после отклонения).

## Закрытие

- [ ] Пользователь подтвердил визуально (**«ок»**).
- [ ] После подтверждения — `git push origin main`.
