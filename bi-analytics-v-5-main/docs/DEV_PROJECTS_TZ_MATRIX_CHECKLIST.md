# Чеклист: отчёт «Девелоперские проекты» — правки от 2026-05-05

**Эталон данных для сверок:** **`bi-analytics-v-5-main/web/`** (MSP `AI/msp_*.csv`, выгрузки 1С `1с_*_dannye.json`, Tessa `tessa_*-*.csv`).

**План повторной приёмки (процесс A–I, пункты 1–5, бэклог):** см. **[`DEV_PROJECTS_QA_VERIFICATION_PLAN.md`](DEV_PROJECTS_QA_VERIFICATION_PLAN.md)** — используйте его как основной чеклист цикла «скрин → вкладка → правка → эталон `web/` → „ок“ → коммит/push → отметка».

---

## Изменения в коде после синхронизации с **main** (2026-05-05, для сверки при проверке)

| Тема | Статус кода | Комментарий для проверки |
|------|-------------|---------------------------|
| Подсветка % (0 %, шкала 0–1 / 0–100) | Реализовано | `_pct_scale_max_from_frame`, `_normalized_pct_0_100`, исключение истинного **0 %** из оранжевого «< 100 %» |
| Область проекта в матрице | Реализовано | `project_label_for_scope` в `build_dev_tz_matrix_rows` — не смешивать сырые имена в одном столбце (напр. Дмитровский / Дмитровский 1) |
| Старт приложения / деплой | Реализовано | Фильтры: `dashboards/ui_quiet.py`; shim `filter_layout.py`; в `project_visualization_app` — `inject_unified_filters_css` из `ui_quiet` |
| Фильтры «как Excel» на всех дашбордах | Не в этом релизе | Отдельное ТЗ; пункт **5.1** в плане приёмки |

**Из скринов заказчика (зафиксировать при приёмке):** расхождение План/Факт по вехе «Готовый продукт» при смешении имён проекта; оранжевый при **0 %**; отсутствие оранжевого там, где должен быть промежуточный %.

---

## Код и логика

- [x] **1. Чекбокс вертикальных дат** — убран; даты только горизонтально (`dashboards/_renderers.py`: `vert_dates = False`; подсказка в админ-JSON обновлена).
- [x] **2а. Предписания — только TESSA** — строка «ПРЕДПИСАНИЯ» из `tessa_tasks_data` с фильтром проекта (`_tessa_counts` + `build_predpisaniya_detail_df`), без падения назад на MSP/`Фаза`.
- [x] **2б. Финансы — как БДДС** — «Выборка ДС» через `try_synthetic_budget_from_1c_dannye` по `reference_1c_dannye`, fallback на прежние обороты (`_dev_matrix_bddds_totals_mln`).
- [x] **3. Дубликаты проектов (Дмитровский)** — в режиме «Все проекты» строки группируются ключом `_control_points_project_group_key`; в фильтре «Проект» — те же группы.
- [x] **4. Оранжевая подсветка / % выполнения** — представитель вехи по минимальному числовому `% complete` **с учётом шкалы столбца** и нормализации; **0 %** не даёт оранжевого «не 100 %»; tooltip на План/Факт при предупреждении.
- [x] **5. РС** — расширены `names_any` / `names_exact_any` / `phase_needles` для задач РС в MSP.
- [x] **6. Отклонения (дней)** — **«План» = базовое окончание, «Факт» = окончание**, отклонение календарных дней; **сужение строк по подписи проекта** в матрице; при расхождениях сверить `web/`.
- [x] **7. Даты DD.MM.YYYY** — `_fmt_date_ru` с `dayfirst=True`; при спорных кейсах — исходная ячейка в CSV + эталон `web/`.
- [x] **8. Жирное начертание** — усилены `font-weight` для «Проект», групп фаз и заголовков вех в CSS матрицы.

---

## Проверка в UI (повторный прогон — отмечать по [`DEV_PROJECTS_QA_VERIFICATION_PLAN.md`](DEV_PROJECTS_QA_VERIFICATION_PLAN.md))

- [ ] Streamlit: **«Девелоперские проекты»**, тема по умолчанию; режим **«Все»**, затем по одному логическому проекту; сверка с эталоном `web/`.
- [ ] Нет UI-чекбокса вертикальных дат; шапка вех читается; оранжевый **не** на **0 %**; на промежуточных **%** оранжевый по ТЗ; tooltip про % на План/Факт.
- [ ] Блок **ПРЕДПИСАНИЯ** — Tessa KindName=`Предписан…` (`web/tessa_*-task*.csv`).
- [ ] **Выборка ДС, млн руб.** — согласована с БДДС / `*_dannye.json`.
- [ ] Вехи **РС**, отклонения дней — строки в `web/AI/msp_*.csv` (базовое окончание, окончание, %, имя задачи, **верный project name**).
- [ ] Старт приложения без ошибки загрузки `dashboards`; отчёт **«Причины отклонений»** открывается.
- [ ] **БДДС / БДР** — по подразделу ниже и по плану приёмки при необходимости.

---

## БДДС / БДР (обороты 1С, `dashboards/_renderers.py`, `dashboards/finance_from_1c.py`, `utils.py`)

- [x] **СтатьяОборотов** — только строки с **лотом и подлотом** в названии (`_turnover_article_has_lot_and_sublot`, фильтр в synthetic budget/BDR).
- [x] **Округление и подписи** — млн руб., **два** знака; сверка `python scripts/verify_web_finance_totals.py` по JSON из `web/` (если скрипт в репозитории).
- [x] **Строка «Итого»** — жирный; «По месяцам» / «Накопительно» по ТЗ.
- [x] **Таблица** — уже́е ячейки, крупнее шрифт в `budget_table_to_html`.
- [x] **Порядок колонок** — Период → Проект → План → Факт → Отклонение (+ опц. БДДС).

---

## Закрытие

- [ ] Пользователь подтвердил визуально (**«ок»**) по плану **DEV_PROJECTS_QA_VERIFICATION_PLAN.md**.
- [ ] После подтверждения — `git commit` + `git push` на **main**.
- [ ] Чекбоксы пункта и раздела «Проверка в UI» отмечены.
