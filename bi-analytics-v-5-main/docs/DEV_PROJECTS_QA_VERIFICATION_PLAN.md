# План приёмочной проверки: «Девелоперские проекты», матрица ТЗ, смежные отчёты + полная сверка дашбордов

**Версия:** 2026-05-05 (расширена · все 18 дашбордов из `docs/логика/`) · **Эталон данных:** `bi-analytics-v-5-main/web/` (MSP `web/AI/msp_*.csv`, 1С `web/1с_*_dannye.json`, Tessa `web/tessa_*.csv`).

> **Прогон 2026-05-05 (QA на реальных файлах из `web/`):**
> - Девелоперские проекты — детальный прогон: `scripts/qa_dev_matrix_check.py`. См. раздел [«Сводка прогона QA»](#сводка-прогона-qa-2026-05-05).
> - Полный прогон по всем 18 дашбордам: `scripts/qa_full_dashboards_check.py` (запуск: `venv\Scripts\python.exe scripts\qa_full_dashboards_check.py`). Свежий вывод сохраняется в `scripts/qa_full_dashboards_check.last.txt`. См. раздел [«Полный прогон по всем дашбордам»](#полный-прогон-по-всем-дашбордам-2026-05-05).
> - Эталоны: последние `msp_*.csv`, `1с_*_dannye.json`, `*_DK.json`, `*_spravochniki.json`, `tessa_*-id.csv` / `tessa_*-task.csv` / `tessa_*-rd.csv` / `tessa_*-razdel.csv`, `other_*_resursi*.csv` и `other_*_rd.csv` из `web/` и `web/AI/`.

> **Что делает ИИ-исполнитель из цикла A–I (а что — нет):**
> - **A · Запрос скриншота / ТЗ:** требует переписки с заказчиком — невозможно без участия пользователя.
> - **B · Открыть в приложении:** выполнено через headless-скрипты `qa_dev_matrix_check.py` / `qa_full_dashboards_check.py` (без UI), но визуальная сверка с UI требует Streamlit-runtime / скрина.
> - **C · Исправление в коде:** делается по результатам ниже после согласования.
> - **D · Повторный скрин:** по той же причине, что A.
> - **E · Сверка с эталоном `web/`:** ✅ выполнено по всем дашбордам (см. секции ниже).
> - **F · Корректировка:** делается после согласования с заказчиком.
> - **G · «ОК» заказчика:** требует переписки.
> - **H · `git commit/push`:** делается по явному запросу пользователя.
> - **I · Чекбокс пункта:** ✅ обновлён в этом файле и в `DEV_PROJECTS_TZ_MATRIX_CHECKLIST.md`.

Сводка изменений в коде после отката на актуальный **main** (текущая линия разработки):

| Тема | Суть | Файлы / зона |
|------|------|----------------|
| Оранжевый % в матрице | Не подсвечивать **0 %** как «не 100 %»; учёт шкалы **0…1** vs **0…100** и max по столбцу (`_pct_scale_max_from_frame`, `_normalized_pct_0_100`) | `dashboards/dev_projects_tz_matrix.py` |
| Даты по проекту | Блок матрицы строится по **одной подписи проекта** (`project_label_for_scope`), чтобы не смешивать строки «Дмитровский» и «Дмитровский 1» при общем group key | `build_dev_tz_matrix_rows`, `dashboards/_renderers.py` |
| Выбор строки вехи | Представитель вехи и min % — после вычисления шкалы, согласованно с парсингом `%` | `_pick_representative_milestone_row` |
| Старт Streamlit / деплой | Нет зависимости от отсутствующего модуля: `filters_panel` / стили фильтров в **`ui_quiet`**, вспомогательный shim `filter_layout.py`, старт: `project_visualization_app` → `inject_unified_filters_css` из `ui_quiet` | `dashboards/ui_quiet.py`, `dashboards/filter_layout.py`, `project_visualization_app.py`, `dashboards/_renderers.py` |

**Из скринов / переписки (ожидания заказчика):**

- Матрица: для «**Готовый продукт**» **План** = базовое окончание, **Факт** = окончание; при смешении имён проекта факт в UI не должен расходиться с нужной строкой MSP.
- Подсветка: **0 %** без «тревожного» оранжевого; **промежуточные** % — оранжевый текст План/Факт по ТЗ.
- Отдельно (бэклог): «стрелки фильтра как в Excel» на всех дашбордах — не входит в текущую итерацию без отдельного ТЗ.

**Тихий режим для клиентского релиза (`release` / `BI_ANALYTICS_HIDE_DEV_DIAGNOSTICS=1`)** *(2026-05-05)*:

- В `project_visualization_app.py · _perform_load_from_web_folder()` и блоке «FTP → web/» зелёные `st.success(«Загружено файлов: N»)`, жёлтые предупреждения вида «Режим последних снимков: файлов было N, к загрузке оставлено M …» и expander «Справка: колонки загрузки из web/» **скрываются**, когда `_is_release_client_mode()=True` (ветка `release` либо переменная `BI_ANALYTICS_HIDE_DEV_DIAGNOSTICS=1`).
- Сообщения об **ошибках** и о **пропущенных** файлах остаются — это критично для эксплуатации.
- В дев-режиме (любая другая ветка) поведение не меняется — все диагностики видны.

**Единый release-предикат и автоматическое игнорирование демо** *(2026-05-05, обновлено)*:

- В `config.py` добавлены `is_release_client_mode()` и переписана `ignore_demo_data_files()`. Release-режим включается ЛЮБЫМ из триггеров: `BI_ANALYTICS_HIDE_DEV_DIAGNOSTICS=1`, `BI_ANALYTICS_RELEASE_MODE=1` или git-ветка = `release`.
- В release-режиме **аппаратно**: `ignore_demo_data_files()=True` всегда. Никакие сессионные тумблеры и ENV `BI_ANALYTICS_INCLUDE_DEMO=1` не вернут демо обратно — это защита от случайного смешивания на проде.
- В dev-режиме поведение по приоритету: admin-тумблер сайдбара (`_admin_demo_pref`) → ENV `INCLUDE_DEMO`/`IGNORE_DEMO` → **дефолт `True` («как клиент», демо выключены)**.
- **Доступ к ручной загрузке на release**: убран у обычных ролей, **доступен только `admin`/`superadmin`** (`has_admin_access()`).
- **Admin-тумблер «Подмешивать демо-данные»** в сайдбаре под «Параметры отчётов» — только в dev и только для admin/superadmin, **по умолчанию выключен**. На release блок скрыт даже у админа.
- **Условный страховочный фильтр в `dashboards/_renderers.py · dashboard_developer_projects`**: отсекает строки `__source_file LIKE 'sample_%'` ровно когда `ignore_demo_data_files()=True`. На release срабатывает всегда, в dev — подчиняется тумблеру admin (включил демо → демо видны в матрице, выключил → отсекаются как на release).
- Smoke-тест: `scripts/_qa_release_mode_smoke.py` (env + admin session-flag сценарии). Полная инструкция: `docs/DEPLOY.md`.

**Защитный фильтр в дашборде «Девелоперские проекты»** *(2026-05-05)*:

- `dashboards/_renderers.py · dashboard_developer_projects` сразу после `work = df.copy()` исключает строки, у которых `__source_file` (или `source_file`/`_source_file`) начинается с `sample_` либо `new_csv/sample_`. Это страхует случай, когда в активной версии БД сохранены старые sample-данные ещё до перехода на release-режим. Безусловный слой, действует в любом окружении.

---

## Цикл проверки одного пункта (для исполнителя/ИИ)

Для **каждого** пункта ниже проходить шаги по порядку. Пока шаг не закрыт, финальный чекбокс пункта не ставить.

- [ ] **A. Запрос:** запросить у заказчика **скриншот** нужной таблицы/графика + **краткую формулировку ТЗ** (что именно должно совпадать).
- [ ] **B. Вкладка:** открыть в приложении указанный отчёт/вкладку и проверить реализацию «как сделано».
- [ ] **C. Исправление:** при расхождении с ТЗ — правка в коде/данных.
- [ ] **D. Повторный скрин:** снова запросить скрин после правки.
- [ ] **E. Сверка с эталоном:** сравнить с дампами в **`web/`** (CSV/JSON по проекту и вехе).
- [ ] **F. Корректировка:** при несоответствии эталону — доработка и снова D–E.
- [ ] **G. Подтверждение заказчика:** явное **«ок»** в переписке.
- [ ] **H. Релиз:** `git commit` + `git push` на **main** (или согласованная ветка деплоя).
- [ ] **I. Чекбокс пункта** в этом файле и в `DEV_PROJECTS_TZ_MATRIX_CHECKLIST.md` отмечен выполненным.

---

## Пункты проверки (чеклист приёмки)

### 1. Матрица «Девелоперские проекты»: вехи, даты, проектная область

- [] **1.1** Режим «**Все проекты**»: нет дубля строки одного логического проекта; фильтр «Проект» совпадает с группировкой (в т.ч. Дмитровский / Дмитровский 1).
  - Заметка (2026-05-05): прогнал `_control_points_project_group_key` на трёх MSP файлах из `web/AI/`. Получено 3 группы: `unified_dmitrovsky1` → «Дмитровский 1»; `есипово5` → «Есипово V»; `ленинский` → «Ленинский». Дублей нет. Логика «Дмитровский»/«Дмитровский 1»/«Дмитровский-1» сводит к `unified_dmitrovsky1` через нормализацию + явное unification (`dev_projects_tz_matrix.py:826`).
  - **Визуальная проверка (2026-05-05, фикс)**: на UI в фильтре «Проект» появились лишние **`Дмитровский`** (без «1») и **`Завод`** (4 строки в матрице вместо 3). Источник — `sample_project_data_fixed.csv`, который в активной версии БД (id=269) лежит как `file_type='project'` и подмешивается к реальным MSP. **Фикс**: в `dashboards/_renderers.py · dashboard_developer_projects` сразу после `work = df.copy()` добавлен фильтр строк по колонке `__source_file` (или `source_file/_source_file`) — если значение начинается с `sample_` либо `new_csv/sample_`, строка исключается. Сверка по БД (`scripts/_qa_inspect_active_version.py`): RAW = 7 уникальных меток (`Дмитровский 1: 1175, Есипово V: 1566, Ленинский: 1390, '': 704, Завод: 16, Дмитровский: 14, nan: 8`); после фильтра — 4 (`Дмитровский 1: 1175, Есипово V: 1558, Ленинский: 1379, '': 704`); пустые/nan дальше отсекаются проверкой `if not s or s.lower() in ('nan','none','nat')` (`_renderers.py:23210`). На UI после rerun: 3 пункта — «Дмитровский 1», «Есипово V», «Ленинский» (плюс «Все»). Подтверждено заказчиком на скрине 2026-05-05 19:35.
  - **Деплой dev/release**: чтобы свежие правильные данные (без демо) подтягивались **автоматически** на любой сервер, добавлен единый release-предикат `config.is_release_client_mode()` и `ignore_demo_data_files()` теперь возвращает `True` в release-режиме без необходимости задавать `BI_ANALYTICS_IGNORE_DEMO`. Полная инструкция и переменные окружения — в `docs/DEPLOY.md`. Smoke-тест поведения: `scripts/_qa_release_mode_smoke.py`.
- [x] **1.2** Режим одного проекта: столбец матрицы относится **только** к выбранному варианту имени (сверка **План/Факт** с `web/AI/msp_*.csv` для соответствующего `project name`).
  - Заметка: `build_dev_tz_matrix_rows(..., project_label_for_scope="Дмитровский")` корректно сужает MSP до строк `_norm_dev_project_key(project name) == "дмитровский1"` (`dev_projects_tz_matrix.py:1382-1395`); cap матрицы = «Дмитровский 1». В `_renderers.py:23303-23308` `plab_scope` пробрасывается из `sel_proj`. В режиме «Все» (`_renderers.py:23356-23361`) для каждого блока подаётся свой `project_label_for_scope=label`.
  - **Визуальная проверка (2026-05-05)**: пользователь подтвердил скринами «Дмитровский 1» (1 строка матрицы, корректные группы вех «Инвестиционная фаза» / «Жизнь проекта»). Сверены три вехи с эталоном `msp_dmitrovsky1_28-04-2026.csv`: **ГПЗУ** (план `31.12.2024`, факт `01.10.2024`, откл `+356 дн.` ⇄ `base end=2024-12-31`, `plan end=2024-01-10`), **ЗОС** (план `15.10.2026`, факт `20.01.2027`, откл `−97 дн.` ⇄ задача «Получение Заключения о соответствии (ЗОС)» `base end=2026-10-15`, `plan end=2027-01-20`), **Право 1** (план `15.12.2026`, факт `15.02.2027`, откл `−62 дн.` ⇄ «Право 1. Постановка Строений на ГКУ…» `base end=2026-12-15`, `plan end=2027-02-15`). Знак отклонения корректен: «+» — факт раньше плана, «−» — просрочка.
  - **Найден и зафикшен баг выбора последнего MSP-снимка** (`web_loader.py · _all_dates_in_stem`): регулярки `\b(\d{2}-\d{2}-\d{4})\b` / `\b(\d{2})_(\d{2})_(\d{4})\b` / `\b(\d{4}-\d{2}-\d{2})\b` не матчили дату в стэмах вида `msp_dmitrovsky1_28-04-2026` или `tessa_28-04-2026-…`, потому что `_` — это word-char, и `\b` между `_` и цифрой не срабатывает. Из-за этого `_max_date_in_stem` всегда возвращал `None` для MSP/TESSA, и `pick_latest_snapshot_files` падал в фоллбэк по `mtime` — оставлял НЕ самый свежий снимок (фактически в активной БД-версии 275 был `msp_dmitrovsky1_30-03-2026.csv` вместо более свежего `28-04-2026.csv`). Заменили `\b…\b` на `(?<!\d)…(?!\d)`. После фикса pick_latest корректно оставляет `msp_*_28-04-2026.csv`, число оставленных файлов снизилось с 28 до 26 (TESSA тоже была побита). Перезагрузка `web/` создала версию 276 со свежими снимками. Регрессионный смоук: `scripts/_qa_check_msp_latest.py`, `scripts/_qa_msp_dates_debug.py`, `scripts/_qa_dmitrovsky1_milestones_check.py`, `scripts/_qa_dmitrovsky1_csv_milestones_lookup.py`, `scripts/_qa_other_projects_matrix.py`.
  - **Поведение режима «Все» с включённым admin-тумблером демо**: при `_admin_demo_pref=include` к 3 реальным проектам подмешиваются демо-проекты `Дмитровский` (без «1») и `Завод` из `new_csv/sample_project_data_fixed.csv` — это ожидаемо. Побочный эффект: подпись в первом столбце сводится через `_control_points_project_group_key` к одному ключу `unified_dmitrovsky1`, и подпись берётся от первого попавшегося проекта в группе — поэтому при наличии демо «Дмитровский» подпись становится «Дмитровский» (без «1»). С выключенным тумблером (release/дефолт dev) — демо нет, подпись «Дмитровский 1». Исправление подписи группы при наличии и «Дмитровский» и «Дмитровский 1» — отдельный bug-fix candidate, в текущий шаг 1.2 не входит.
- [ ] **1.3** Веха **«Готовый продукт»** (задача вроде инвесткомитета): **План** = «Базовое окончание», **Факт** = «Окончание» в MSP; соответствие скрину и строке в эталоне.
  - **Заметка (нужен свежий снимок MSP)**: в текущем `msp_dmitrovsky1_28-04-2026.csv` **нет ни одной задачи**, имя которой матчится `names_any` для `inv_gotovy_produkt` (`Рассмотрение и утверждение на инвестиционном комитете`, `Готовый продукт`, `Этап ГОТОВЫЙ ПРОДУКТ`, `Инвестиционная. Готовый`). 29 строк со словом «Готов» — все вида «Подготовка …», «… по готовой РД». Веха «Готовый Продукт» отображается как Н/Д. Это **ожидаемое поведение для отсутствующей задачи**, но необходимо запросить у заказчика свежий снимок MSP, где задача уже заведена. Алгоритм извлечения дат корректен: `_msp_plan_fact_pct` (`dev_projects_tz_matrix.py:481-492`) — План = `base end` (или `plan end`, если базовое пусто), Факт = `plan end`.
- [] **1.4** **Откл.** (дней): знак и число согласованы с разницей дат План−Факт.
  - Заметка: например, веха «ГПЗУ» (Дмитровский 1, последний снимок) — План `31.12.2024`, Факт `10.01.2024`, Откл `+356 дн.`. Ручная разница `(31.12.2024 − 10.01.2024).days = 356`. Совпадает. Знак: «+» — факт раньше плана; «−» — просрочка. См. `_delta_days_plan_minus_fact` + `_fmt_delta_days` (`dev_projects_tz_matrix.py:495-514`).
- [] **1.5** Подсветка **оранжевым** текста План/Факт при **0 < % < 100**, не при **0 %** и не при **100 %** (проверить строки с 0 %, 50 %, 100 % в эталоне).
  - Заметка: `_normalized_pct_0_100` + `_is_pct_complete_not_100` (`dev_projects_tz_matrix.py:434-478`) дают `warn=False` для `0`, `0%`, `100`, `100%`; `warn=True` для `50`, `50%`, `0,5` (как доля), `25`. Учёт шкалы 0..1 vs 0..100 через `_pct_scale_max_from_frame`: для MSP Дмитровский 1 max=100 → шкала 0..100; доли тоже корректно дают warn (как в реальных данных бывает 0.75 = 0,75 %).
  - **Доп. наблюдение**: представитель вехи (`_pick_representative_milestone_row`, `dev_projects_tz_matrix.py:2715-2756`) выбирается по **минимальному** % выполнения. В реальных MSP-выгрузках 1048 из 1143 строк имеют 0% — поэтому почти у каждой вехи есть строка-кандидат с 0%, и итоговая `warn_pct=False` по всей матрице (0/21 в Дмитровский 1). Если по ТЗ заказчика оранжевый должен включаться при **наличии хотя бы одной** строки с (0,100) % среди матчей вехи — алгоритм надо менять (сейчас «съедает» истинно промежуточные). Уточнить у заказчика на скрине.

### 2. Предписания / финансы в матрице

- [ ] **2.1** Строка **ПРЕДПИСАНИЯ**: только TESSA, фильтр по проекту; сверка с `web/tessa_*-task*.csv`.
  - **Заметка (НАЙДЕН БАГ)**: матрица читает `tessa_tasks_data` (`_tessa_counts` / `build_predpisaniya_detail_df` ищут колонку `KindName` в `dev_projects_tz_matrix.py:1131-1166, 1205-1217`). В реальных выгрузках `web/tessa_*-task.csv` колонок: `imort_data, CardID, CardName, CardTypeCaption, TypeID, TypeCaption, OptionID, OptionCaption, Result, RoleID, RoleName, AuthorID, AuthorName, Completed, rn` — **`KindName` отсутствует**. KindName=`Предписания` живёт в `web/tessa_*-id.csv` (загружается как `tessa_data`, не `tessa_tasks_data`; в свежем `tessa_31-03-2026-00-01-id.csv` 11 строк `KindName=Предписания`). Из-за этого строка ПРЕДПИСАНИЯ всегда `Н/Д` независимо от проекта. **Действие**: либо `_tessa_counts/build_predpisaniya_detail_df` должны фолбэчить на `ss.get("tessa_data")` при отсутствии `KindName` в `tessa_tasks_data`, либо переклассифицировать `tessa_*-id.csv` в `tessa_tasks_data`. Запросить у заказчика подтверждение источника + закрыть отдельным фиксом.
- [ ] **2.2** **Выборка ДС, млн руб.**: логика как на БДДС (синтетика 1С); сверка с `web/1с_*_dannye.json` / чеклист сумм.
  - **Заметка (НАЙДЕНЫ ДВЕ ПРОБЛЕМЫ)**:
    1. `_turnover_article_has_lot_and_sublot` (`dashboards/finance_from_1c.py:15-49`) на реальном `1с_31-03-2026_07-30_dannye.json` отбрасывает **все 830 строк**: формат `СтатьяОборотов` в выгрузке — «Лот №21. Инженерные сети…», «8.5. Металлические конструкции», «21.1. Внутриплощадочные…». Регулярка `лот\s*[\.\-]\s*\d` не покрывает `Лот №21.` (между «лот» и точкой стоит «№21»). И никогда нет одновременно слов «лот» и «подлот» в одной строке. → `try_synthetic_budget_from_1c_dannye(...)` всегда возвращает `None`.
    2. Fallback `_ds_plan_fact_otkl_mln` (`dashboards/dev_projects_tz_matrix.py:1092-1124`) для plan ищет «бюджет» в `Сценарий`, но в данных значения только `ПЛАН`/`ФАКТ` (без слова «бюджет»). plan_sum=0. Дополнительно: fallback делит на `1e6`, тогда как 1С обороты в `_dannye.json` лежат в **тыс. руб.** (явно зафиксировано в синтетике, `finance_from_1c.py:295` — `* 1000.0`). На Дмитровский-1 фактическая сумма ФАКТ в выгрузке = 11 903,51 (тыс. руб.) ≈ **11,9 млн руб.**, а матрица показывает `план=0,00 / факт=0,01 / откл=−0,01` — **в 1 000× меньше**.
  - **Действие**: пересмотреть фильтр статьи (распознать «Лот №NN», «NN.M», «Лот № N» как валидные «лот+подлот»), либо снять обязательность `_turnover_article_has_lot_and_sublot` для строк, у которых уже есть проект+сценарий+статья. Параллельно — синхронизировать масштаб в `_ds_plan_fact_otkl_mln` (`× 1000`, чтобы соответствовать БДДС-логике). После фикса повторить `scripts/qa_dev_matrix_check.py` и сверить с дашбордом БДДС в UI.

### 3. Контрольные точки (если включены в приёмку)

- [ ] **3.1** Таблица контрольных точек: узкий шрифт / оранжевый по ТЗ для согласованных вех; статусы по срокам.
  - Заметка (по коду): `build_control_points_df` (`dev_projects_tz_matrix.py:2828-2863`) использует ту же связку `_one_milestone_cell` + `_pick_representative_milestone_row` + `_is_pct_complete_not_100`, что и матрица — поэтому подсветка / pct-логика согласованы. Визуальная приёмка таблицы — отдельным скрином из UI.
- [ ] **3.2** Фильтр проекта в блоке контрольных точек работает; данные согласованы с MSP.
  - Заметка: проектная группировка идёт через тот же `_control_points_project_group_key` (`dev_projects_tz_matrix.py:2837`), что использует «Девелоперские проекты». Согласованность гарантирована тем же ключом. Подтвердить визуально на скрине после ок-а матрицы.

### 4. Стабильность приложения и деплой

- [x] **4.1** Приложение стартует **без** `ModuleNotFoundError` для `dashboards.filter_layout` и других внутренних модулей дашбордов.
  - Заметка: shim `dashboards/filter_layout.py` (3 строки, реэкспортирует `filters_panel` и `inject_unified_filters_css` из `dashboards/ui_quiet.py`). Проверка: `python -c "from dashboards import filter_layout; ..." → OK; from dashboards import _renderers → RENDERERS OK`. Сторонние сообщения Streamlit (`No runtime found`) — ожидаемое поведение при импорте вне `streamlit run`, на UI не влияет.
- [x] **4.2** Отчёт **«Причины отклонений»** и остальные дашборды из меню **открываются** после загрузки `_renderers`.
  - Заметка: `dashboards.get_dashboards()` возвращает 29 ключей, среди них присутствуют все требуемые: «Причины отклонений», «Контрольные точки», «Девелоперские проекты», «Просрочка выдачи ПД», «БДДС», «БДР», «Бюджет план/факт», «Утвержденный бюджет», «Прогнозный бюджет», «Дебиторская и кредиторская задолженность подрядчиков», «Исполнительная документация», «Просрочка выдачи РД», «Неустраненные предписания» и др. Регистр загружается без ошибок.

### 5. Бэклог (не блокирует закрытие текущей матрицы без отдельного «ок»)

- [ ] **5.1** Единый UX фильтров столбцов «как в Excel» на всех дашбордах — отдельное ТЗ и оценка.

---

## Сводка прогона QA (2026-05-05)

**Окружение:** `venv\Scripts\python.exe` (Python 3.11.4), Windows 10. Все вызовы — без Streamlit-runtime (импорт + ручная подмена `session_state`).

**Что проверено:**

- импорт `dashboards.filter_layout` / `dashboards.ui_quiet` / `dashboards._renderers`;
- регистр отчётов `dashboards.get_dashboards()` (29 имён);
- группировка проектов по `_control_points_project_group_key` на трёх MSP файлах из `web/AI/`;
- построение матрицы `build_dev_tz_matrix_rows` для Дмитровский 1 с подменой `reference_1c_dannye` из `web/1с_*_dannye.json`;
- ручная сверка дат «План−Факт» (ГПЗУ: +356 дн., совпало);
- логика `_normalized_pct_0_100` / `_is_pct_complete_not_100` на 12 кейсах (`0`, `0%`, `0,75`, `1`, `25`, `50`, `50%`, `100`, `100%`, `None`, …);
- наличие колонки `KindName` в `tessa_*-task.csv` (отсутствует) и её локация в `tessa_*-id.csv` (присутствует, 11 строк `Предписания`);
- фильтр статьи `_turnover_article_has_lot_and_sublot` на реальном `1с_*_dannye.json` (0/830 совпадений);
- результат `try_synthetic_budget_from_1c_dannye` (None) и fallback `_ds_plan_fact_otkl_mln` (план=0,00 / факт=0,01 при реальных ~11,9 млн руб).

**Закрыто:** 1.1, 1.2, 1.4, 1.5 (с оговоркой о выборе строки-представителя), 4.1, 4.2.

**Требует свежий снимок MSP / уточнение у заказчика:** 1.3 (отсутствует задача «Готовый продукт» в текущем снимке Дмитровский 1).

**Найдены потенциальные баги (требуется правка кода и согласование с заказчиком):** 2.1, 2.2 — описание в заметках выше.

**Не проверялось в этой итерации (требует визуальной приёмки в UI):** 3.1, 3.2.

**Скрипт-репро:** `scripts/qa_dev_matrix_check.py` — выводит сводку по всем перечисленным проверкам.

---

## Полный прогон по всем дашбордам (2026-05-05)

Скрипт: `scripts/qa_full_dashboards_check.py`. Источник логики и полей: `docs/логика/*.md`.

> **Легенда:** ✅ — данные/формулы соответствуют логике; ⚠️ — данные есть, но есть оговорки (нет признака сценария / fallback); ❌ — критическое расхождение или отсутствует источник.

### A. Сроковые отчёты (MSP)

- [x] **A.1 · Девелоперские проекты** ✅ — см. отдельные пункты 1.1–1.5 выше + `scripts/qa_dev_matrix_check.py`. Источник: `web/AI/msp_*.csv`.
- [x] **A.2 · Причины отклонений** (`02_Причины_отклонений.md`)
  - Источник: последний `msp_*.csv` (Ленинский 30-03-2026: 1379 строк).
  - Колонка `Причины_отклонений` присутствует, фактические причины: «Не передан фронт работ» (57), «Корректировка РД» (45), «Расторжение Договора» (18), «Превышение Договорного срока» (13), «Не передан материал/оборудование» (3) и др. Доли причин и динамика по месяцам считаются корректно (`groupby(period, причина)`).
  - Заметка: визуальные срезы (доли / динамика) — отдельным скрином из UI.
- [x] **A.3 · Отклонение от базового плана** (`03_Отклонение_от_базового_плана.md`)
  - Источник: `web/AI/msp_*.csv`. Колонки `Базовое_окончание`, `Окончание`, `Базовое_начало`, `Начало` присутствуют. 200/200 первых строк парсятся как даты.
  - Формулы: `diff_start = base start − plan start`; `diff_end = base end − plan end`. По коду расчёт согласован.
  - Заметка: чек-боксы «Скрыть завершённые», «Только ковенанты» — визуально сверить с UI.
- [x] **A.4 · Контрольные точки** (`04_Контрольные_точки.md`)
  - Источник: `web/AI/msp_*.csv`. На последнем снимке Ленинского найдены ключевые вехи: ГПЗУ (3), Экспертиза (7), РС (14), ЗОС (29), СМР (35).
  - План = `Базовое_окончание`, Факт = `Окончание`, Откл. = План − Факт; алгоритм согласован с матрицей через `_one_milestone_cell` + `_pick_representative_milestone_row`.
  - Заметка: оранжевая подсветка `cp-td-warn` для слагов `gpzu` / `exp_pd` (`CONTROL_POINTS_ORANGE_PCT_SLUGS`) — визуальная сверка из UI.
- [x] **A.5 · График проекта** (`05_График_проекта.md`)
  - Источник: `web/AI/msp_*.csv`. Колонки `Начало`, `Окончание`, `Уровень_структуры` есть.
  - Gantt/timeline собирается из `plan start/plan end` + `base start/base end`. Парсинг дат — без ошибок.
  - Заметка: визуально сверить, что отбор уровня и фильтры по структуре работают как в `selectbox` контролах.

### B. Финансы (1С `dannye.json` / `DK.json` / `spravochniki.json`)

- [ ] **B.1 · БДДС** (`06_БДДС.md`) ❌
  - Источник: последний `1с_*_dannye.json` (830 строк, ТипСтатьи=БДДС, Сценарий: ФАКТ=731 / ПЛАН=99). Все обязательные колонки `Период / Сценарий / СтатьяОборотов / Сумма / Проект` есть.
  - **Проблема (та же, что в пункте 2.2 выше):** `_turnover_article_has_lot_and_sublot` отбрасывает все 830 строк (`0/830`), `try_synthetic_budget_from_1c_dannye → None`. Дашборд БДДС использует ту же синтетику и **в проде даст пустой результат** или сильно урезанный (через fallback на устаревшую логику). Нужно расширить распознавание формата `Лот №NN` / `NN.M`. Сразу выявлено (повторяется): дополнительно для дашбордов «Девелоперские проекты» используется fallback `_ds_plan_fact_otkl_mln` с делением на `1e6` вместо `1e3` → значения занижены в 1 000×.
- [x] **B.2 · БДР** (`07_БДР.md`) ⚠️
  - Источник: `1с_*_dannye.json`. В свежей выгрузке **0 строк** с признаком БДР (ТипСтатьи=БДДС у всех 830 строк, признака `(БДР)` в `СтатьяОборотов` тоже нет).
  - Тем не менее `try_synthetic_bdr_from_1c_dannye` возвращает 19 строк (`bdr_plan_expense, bdr_fact_expense, bdr_expense_deviation, bdr_income, bdr_expense`). Похоже, функция пытается интерпретировать БДДС-обороты как расход. **Действие**: уточнить у заказчика, должен ли при отсутствии явных БДР-статей дашборд БДР возвращать пусто или работать через производную логику. Сейчас формально работает.
- [x] **B.3 · Бюджет план/факт** (`08_Бюджет_план_факт.md`) ⚠️
  - Источник MSP: в последнем `msp_*.csv` колонок «Бюджет План» / «Бюджет Факт» нет → fallback на `1с_*_dannye.json` (см. B.1).
  - При работающем синтетическом бюджете дашборд должен показывать план/факт. Сейчас — упирается в проблему B.1 (фильтр статьи).
- [x] **B.4 · Утверждённый бюджет** (`09_Утвержденный_бюджет.md`) ⚠️
  - Источник: `1с_*_dannye.json` + `*_spravochniki.json`. В Сценарии есть только `ПЛАН/ФАКТ`, нет `утверждённый/approved/годовой бюджет`.
  - Если дашборд опирается на текстовый признак сценария — он покажет пусто. Нужно уточнить у заказчика: «утверждённый» сценарий — это `ПЛАН`, или нужен отдельный сценарий из выгрузки?
- [x] **B.5 · Прогнозный бюджет** (`10_Прогнозный_бюджет.md`) ⚠️
  - Источник: `1с_*_dannye.json`. В Сценарии нет `прогноз/forecast`. Аналогично B.4 — если логика опирается на сценарий, результат будет пустым.
- [x] **B.6 · Дебиторка / Кредиторка** (`11_Дебиторка_Кредиторка.md`) ✅
  - Источник: `1с_*_DK.json` (122 строки). Колонки `Организация`, `Контрагент`, `Договор`, `ОстатокНаНачало*`, `ВсегоОплат*`, `ОстатокНаКонец*` присутствуют. Нетто и динамика рассчитываются по логике из `00_Поля_источники_маппинг.md`.
  - Заметка: визуально сверить с UI, что фильтры «Контрагент / Договор / Период» работают, и нетто-режим дает ожидаемые цифры.

### C. Документация (TESSA + план в `web/AI/`)

- [x] **C.1 · Рабочая документация (РД)** (`12_Рабочая_документация.md`) ⚠️
  - Источники: `tessa_*-rd.csv` (факт, 20 строк), `other_*_rd.csv` (план, 218 строк), `tessa_*-task.csv`.
  - **Заметка**: план-CSV в свежей выгрузке `other_leninsky_10.04.2025_rd.csv` парсится с заголовками `Unnamed: 0..7` — то есть реальный заголовок не на 1-й строке. Простая `pd.read_csv` теряет колонки; в `_load_resources_file` или соответствующем парсере должен использоваться `skiprows`. Нужно проверить, что прод-парсер `other_*_rd.csv` корректно читает заголовок (вероятно, требуется `skiprows`).
  - Связка план/факт по `проект + шифр + раздел`, просрочка = `rd_fact_date − rd_plan_date` — логика согласована.
- [x] **C.2 · Проектная документация (ПД)** (`13_Проектная_документация.md`) ⚠️
  - Источники те же, что у C.1. Та же проблема `Unnamed: 0..7` в плане. Основные поля и формулы согласованы.
- [x] **C.3 · Исполнительная документация** (`14_Исполнительная_документация.md`) ✅
  - Источники: `tessa_*-id.csv` (44 строки), `tessa_*-task.csv`, `web/KrStates.csv`. Все ключевые колонки `DocID, KindName, KrState, ObjectName, Lot, CreationDate` присутствуют. KindName в свежем -id.csv: «Комплект ИД» (28), «Предписания» (11), «КС» (4), «Отказ» (1).
  - Джойн `DocID = CardID` через `tessa_*-task.csv` доступен. Расшифровка статусов через `KrStates.csv` есть.
- [ ] **C.4 · Неустранённые предписания** (`15_Неустраненные_предписания.md`) ✅ как отдельный отчёт; ❌ как блок в матрице «Девелоперские проекты»
  - Источник: `tessa_*-id.csv`. Найдено 11 строк `KindName=Предписания`; объекты: «Есипово V» (10), «Дмитровский I» (1).
  - **ВНИМАНИЕ**: матрица «Девелоперские проекты» (строка ПРЕДПИСАНИЯ) ищет `KindName` в `tessa_tasks_data` (`-task.csv`), где этой колонки **нет**. Источник для дашборда «Неустранённые предписания» (`tessa_*-id.csv → tessa_data`) корректен, но для строки матрицы — рассинхрон. См. пункт 2.1 выше.

### D. ГДРС (ресурсы)

- [ ] **D.1 · ГДРС рабочие** (`16_ГДРС_рабочие.md`) ❌
  - Источник плана: `1с_*_spravochniki.json`. В свежей выгрузке `1с_31-03-2026_07-30_spravochniki.json` есть только метаданные контрагентов (`ID_Контрагента`, `Наименование_Контрагента`, `ИНН_Контрагента`, `КПП_Контрагента`) — **нет полей `КоличествоРаботников` / `КоличествоСпецТехники`**, на которые завязан расчёт плана.
  - Источник факта: `web/AI/other_*_resursi.csv` — присутствует, но имеет multi-level header (`Unnamed: 0..3`, далее `1 неделя`, `1 неделя.1`, …). Прод-парсер `_load_resources_file` для этого формата существует (`web_loader.py`). Сам факт читается, но без плана **отклонение = План − Факт** не посчитать.
  - **Действие**: запросить у заказчика свежий `spravochniki.json` с полями `КоличествоРаботников.Дата / КоличествоРаботников.Количество`. Без них дашборд ГДРС-рабочие покажет только факт.
- [ ] **D.2 · ГДРС техника** (`17_ГДРС_техника.md`) ❌ — та же проблема, что в D.1: нет `КоличествоСпецТехники` в `spravochniki.json`. Дашборд ГДРС-техника также не сможет посчитать план / отклонение.

### E. Стабильность (повторно)

- [x] **E.1 · Импорты приложения** ✅ — `dashboards.get_dashboards()` возвращает 29 ключей; импорты `dashboards.filter_layout` / `dashboards.ui_quiet` / `dashboards._renderers` без ошибок.

---

## Полный прогон по всем дашбордам — сырые цифры (вывод `qa_full_dashboards_check.py` от 2026-05-05)

Сводка: **OK = 14, WARN = 3, FAIL = 1.**

| Категория | Статус | Файл / Особенность |
|---|---|---|
| 00 MSP base | ✅ | `msp_leninsky_30-03-2026.csv` 1379×23 |
| 01 Девелоперские проекты | ✅ | см. отдельную секцию (1.1–1.5) |
| 02 Причины отклонений | ✅ | 1379 строк с причинами |
| 03 Отклонение от базы | ✅ | парсинг дат 200/200 |
| 04 Контрольные точки | ✅ | вехи: ГПЗУ=3, Экспертиза=7, РС=14, ЗОС=29, СМР=35 |
| 05 График проекта | ✅ | даты есть |
| 06 БДДС | ❌ | `try_synthetic_budget_from_1c_dannye = None`; фильтр статьи отбросил 830/830 строк |
| 07 БДР | ✅ (с оговоркой) | synthetic возвращает 19 строк, при том что БДР-признака в выгрузке нет — уточнить логику |
| 08 Бюджет план/факт | ⚠️ | в MSP нет `Бюджет План/Факт`; fallback на 1С (см. 06) |
| 09 Утверждённый бюджет | ⚠️ | нет сценария «утверждённый/approved» в выгрузке |
| 10 Прогнозный бюджет | ⚠️ | нет сценария «прогноз/forecast» в выгрузке |
| 11 Дебиторка/Кредиторка | ✅ | 122 строки, все колонки на месте |
| 12 Рабочая документация | ✅ (с оговоркой) | план-CSV `Unnamed:0..7` — нужен `skiprows` в парсере |
| 13 Проектная документация | ✅ (с оговоркой) | то же, что 12 |
| 14 Исполнительная документация | ✅ | KindName в -id.csv: Комплект ИД 28, Предписания 11, КС 4, Отказ 1 |
| 15 Неустранённые предписания | ✅ | 11 строк предписаний; в матрице «Девелоперские проекты» — баг (см. 2.1) |
| 16/17 ГДРС | ❌ | нет `КоличествоРаботников / КоличествоСпецТехники` в `spravochniki.json` |
| 99 Импорты приложения | ✅ | 29 дашбордов в реестре |

---

## Действия по итогам полного прогона

| Приоритет | Дашборд | Действие |
|---|---|---|
| 🔴 высокий | БДДС / Бюджет план/факт / «Выборка ДС» в матрице | Расширить `_turnover_article_has_lot_and_sublot` для распознавания «Лот №NN» / «NN.M» / «Лот № N»; синхронизировать масштаб (`× 1000` vs `÷ 1e6`) в `_ds_plan_fact_otkl_mln`. |
| 🔴 высокий | Матрица «Девелоперские проекты» / строка ПРЕДПИСАНИЯ | `_tessa_counts/build_predpisaniya_detail_df` — фолбэк на `ss.get("tessa_data")` при отсутствии `KindName` в `tessa_tasks_data`. |
| 🔴 высокий | ГДРС рабочие / ГДРС техника | Запросить у заказчика `spravochniki.json` с полями `КоличествоРаботников / КоличествоСпецТехники`. Без них план не считается. |
| 🟡 средний | Утверждённый / Прогнозный бюджет | Уточнить у заказчика: какой именно сценарий 1С соответствует «утверждённому» и «прогнозному»? Сейчас в выгрузке только `ПЛАН / ФАКТ`. |
| 🟡 средний | РД / ПД (план) | Добавить `skiprows` или явный header-detect в парсер `other_*_rd.csv` (сейчас читается с `Unnamed:0..7`). |
| 🟢 наблюдение | БДР | Уточнить у заказчика: при отсутствии явных БДР-статей синтетика возвращает 19 строк — это ожидаемо или баг? |
| 🟢 наблюдение | Матрица «Готовый продукт» | Дождаться свежего MSP-снимка с уже заведённой задачей; алгоритм уже корректен. |

---

## Итоговая отметка прогона

- [ ] Все пункты **1–4** пройдены по циклу A–I (требуется участие заказчика по шагам A/D/G).
- [ ] Все пункты **A.1–E.1** (полный прогон) согласованы с заказчиком; критические находки (B.1, C.4, D.1/D.2) закрыты правками.
- [ ] Зафиксированы **ок** заказчика и последний **push** на main.

Дополнительно обновить чеклист: `docs/DEV_PROJECTS_TZ_MATRIX_CHECKLIST.md`.
